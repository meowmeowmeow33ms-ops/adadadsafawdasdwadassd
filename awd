type ControlHandle__DARKLUA_TYPE_a = {Set: (value:any, silent:boolean?) -> (), Get: () -> any}
type ControlRegistry__DARKLUA_TYPE_b = {[string]: ControlHandle__DARKLUA_TYPE_a}
type ChangedListener__DARKLUA_TYPE_c = (path:string, value:any) -> ()
type AutoTotemConfig__DARKLUA_TYPE_d = {Enabled: boolean, Health: number, Mode: string, ModeColor: Color3, ModeColorEnabled: boolean, OnlyCombat: boolean, CombatColor: Color3, CombatColorEnabled: boolean, Packet: boolean, DistanceMin: number, DistanceMax: number, Delay: number, Priority: string, HotbarOnly: boolean, SmartSwitch: boolean, SwitchDelay: number}
type MovementConfig__DARKLUA_TYPE_e = {Bhop: boolean, Fly: boolean}
type ThemeConfig__DARKLUA_TYPE_f = {Style: string}
type Config__DARKLUA_TYPE_g = {AutoTotem: AutoTotemConfig__DARKLUA_TYPE_d, Movement: MovementConfig__DARKLUA_TYPE_e, Theme: ThemeConfig__DARKLUA_TYPE_f}
type BindMode__DARKLUA_TYPE_h = 'Toggle' | 'Hold'
type Bind__DARKLUA_TYPE_i = {Key: Enum.KeyCode, Mode: BindMode__DARKLUA_TYPE_h, Path: string, Name: string, row: Frame?, keyLabel: TextLabel?, modeLabel: TextLabel?}
type NavDefinition__DARKLUA_TYPE_j = {name: string, icon: string, iconHeight: number}
type ModuleDefinition__DARKLUA_TYPE_k = {name: string, caret: boolean?}
type NavItemRefs__DARKLUA_TYPE_l = {frame: Frame, icon: ImageLabel, label: TextLabel}
type Binding__DARKLUA_TYPE_m = {instance: Instance, property: string, token: string}
type WatermarkRefs__DARKLUA_TYPE_n = {Frame: Frame, FPSLabel: TextLabel, PingLabel: TextLabel}
type ShadowHandle__DARKLUA_TYPE_o = {Render: (self:ShadowHandle__DARKLUA_TYPE_o, visible:boolean, strength:number?) -> ()}
type MainWindowRefs__DARKLUA_TYPE_p = {Window: Frame, Scale: UIScale, LeftPanel: Frame, Logo: ImageLabel, TopBar: Frame, SearchBox: TextBox, PresetButton: Frame, PresetButtonLabel: TextLabel, CrumbSection: TextLabel, CrumbElement: TextLabel, MainTitle: TextLabel, ExtraTitle: TextLabel}
type HorizontalMode__DARKLUA_TYPE_q = 'button' | 'container' | 'fixed'
type Options__DARKLUA_TYPE_r = {tweenInfo: TweenInfo?, horizontal: HorizontalMode__DARKLUA_TYPE_q?, left: number?, right: number?, paddingY: number?}
type TabIndicatorController__DARKLUA_TYPE_s = {moveTo: (self:TabIndicatorController__DARKLUA_TYPE_s, targetButton:GuiObject, instant:boolean?) -> (), refresh: (self:TabIndicatorController__DARKLUA_TYPE_s) -> (), destroy: (self:TabIndicatorController__DARKLUA_TYPE_s) -> ()}
type NavRefs__DARKLUA_TYPE_t = {items: {[string]: NavItemRefs__DARKLUA_TYPE_l}, highlight: Frame, indicator: ImageLabel, pill: TabIndicatorController__DARKLUA_TYPE_s, arrow: TabIndicatorController__DARKLUA_TYPE_s, order: {string}}
type PanelRefs__DARKLUA_TYPE_u = {main: Frame, extra: Frame}
type TabsConfig__DARKLUA_TYPE_v = {navRefs: NavRefs__DARKLUA_TYPE_t, crumbSection: TextLabel, crumbElement: TextLabel, mainTitle: TextLabel, extraTitle: TextLabel, tabModules: {[string]: {ModuleDefinition__DARKLUA_TYPE_k}}, panels: {[string]: PanelRefs__DARKLUA_TYPE_u}, onTabSelected: ((tab:string) -> ())?, onModuleSelected: ((tab:string, moduleName:string) -> ())?}
type TabsController__DARKLUA_TYPE_w = {selectTab: (self:TabsController__DARKLUA_TYPE_w, tab:string) -> (), selectModule: (self:TabsController__DARKLUA_TYPE_w, tab:string, moduleName:string) -> (), currentTab: () -> string, currentModule: (tab:string) -> string?}
type DropdownPopupApi__DARKLUA_TYPE_x = {open: (anchor:GuiObject, options:{string}, current:string, onPick:(option:string) -> ()) -> (), close: () -> ()}
type ColorPickerApi__DARKLUA_TYPE_y = {open: (swatch:Frame, path:string, title:string) -> (), close: () -> ()}
type CaptureCallback__DARKLUA_TYPE_z = (keyCode:Enum.KeyCode) -> ()
type PendingCapture__DARKLUA_TYPE_A = {onCaptured: CaptureCallback__DARKLUA_TYPE_z, onCancelled: (() -> ())?}
type KeybindPickerApi__DARKLUA_TYPE_B = {open: (anchorRow:Frame, path:string, name:string) -> (), close: () -> ()}
type ModuleSelectorApi__DARKLUA_TYPE_C = {open: (tab:string, navFrame:Frame) -> (), close: () -> ()}
type PopupsApi__DARKLUA_TYPE_D = {Dropdown: DropdownPopupApi__DARKLUA_TYPE_x, ColorPicker: ColorPickerApi__DARKLUA_TYPE_y, KeybindPicker: KeybindPickerApi__DARKLUA_TYPE_B, ModuleSelector: ModuleSelectorApi__DARKLUA_TYPE_C, closeAll: () -> ()}
type PresetsApi__DARKLUA_TYPE_E = {toggle: () -> ()}
type PresetEntry__DARKLUA_TYPE_F = {name: string, snapshot: Config__DARKLUA_TYPE_g, row: Frame, nameLabel: TextLabel, editIcon: ImageLabel, deleteIcon: ImageLabel}
type KeybindListApi__DARKLUA_TYPE_G = {addBind: (path:string, name:string, key:Enum.KeyCode, mode:BindMode__DARKLUA_TYPE_h) -> ()}
type NotificationsApi__DARKLUA_TYPE_H = {notify: (title:string, body:string?, duration:number?) -> ()}
type Toast__DARKLUA_TYPE_I = {frame: Frame, shadow: ShadowHandle__DARKLUA_TYPE_o, height: number, leaving: boolean}
type SearchEntry__DARKLUA_TYPE_J = {row: Frame, divider: Frame?, text: string}
type ToggleOpts__DARKLUA_TYPE_K = {dots: boolean?, bindName: string?, swatchOffset: number?, swatchPath: string?}
type PopupsApi__DARKLUA_TYPE_L = {ColorPicker: {open: (swatch:Frame, path:string, title:string) -> ()}, KeybindPicker: {open: (anchorRow:Frame, path:string, name:string) -> ()}}
type DropdownOpts__DARKLUA_TYPE_M = {swatchOffset: number?, swatchPath: string?}
type PopupsApi__DARKLUA_TYPE_N = {ColorPicker: {open: (swatch:Frame, path:string, title:string) -> ()}, Dropdown: {open: (anchor:GuiObject, options:{string}, current:string, onPick:(option:string) -> ()) -> (), close: () -> ()}}
type AutoTotemPanelRefs__DARKLUA_TYPE_O = {main: Frame, extra: Frame}
type ThemePanelRefs__DARKLUA_TYPE_P = {main: Frame, extra: Frame}
type WindowManagerApi__DARKLUA_TYPE_Q = {isOpen: () -> boolean, setOpen: (open:boolean) -> ()}

local __DARKLUA_BUNDLE_MODULES = {
    cache = {}::any,
}

do
    do
        local function __modImpl()
            local Connections = {}
            local active: {RBXScriptConnection} = {}

            function Connections.bind<T...>(
                signal: RBXScriptSignal<T...>,
                callback: (T...) -> ()
            ): RBXScriptConnection
                local connection = signal:Connect(callback)

                table.insert(active, connection)

                return connection
            end
            function Connections.disconnectAll()
                for _, connection in active do
                    pcall(function()
                        connection:Disconnect()
                    end)
                end

                table.clear(active)
            end

            return Connections
        end

        function __DARKLUA_BUNDLE_MODULES.a(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.a

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.a = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            return {}
        end

        function __DARKLUA_BUNDLE_MODULES.b(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.b

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.b = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local Types = __DARKLUA_BUNDLE_MODULES.b()
            local State = {}
            local Config: Config__DARKLUA_TYPE_g = {
                AutoTotem = {
                    Enabled = false,
                    Health = 40,
                    Mode = 'Smart',
                    ModeColor = Color3.fromRGB(236, 236, 244),
                    ModeColorEnabled = true,
                    OnlyCombat = true,
                    CombatColor = Color3.fromRGB(236, 236, 244),
                    CombatColorEnabled = true,
                    Packet = false,
                    DistanceMin = 15,
                    DistanceMax = 56,
                    Delay = 100,
                    Priority = 'Health',
                    HotbarOnly = true,
                    SmartSwitch = false,
                    SwitchDelay = 200,
                },
                Movement = {
                    Bhop = false,
                    Fly = false,
                },
                Theme = {
                    Style = 'Gray',
                },
            }
            local Controls: ControlRegistry__DARKLUA_TYPE_b = {}
            local listeners: {ChangedListener__DARKLUA_TYPE_c} = {}

            State.Config = Config
            State.Controls = Controls

            local function pathGet(path: string): any
                local node: any = Config

                for key in string.gmatch(path, '[^%.]+')do
                    if typeof(node) ~= 'table' then
                        return nil
                    end

                    node = node[key]
                end

                return node
            end
            local function pathSet(path: string, value: any)
                local keys = {}

                for key in string.gmatch(path, '[^%.]+')do
                    table.insert(keys, key)
                end

                local node: any = Config

                for i = 1, #keys - 1 do
                    node = node[keys[i] ]
                end

                node[keys[#keys] ] = value
            end

            function State.get(path: string): any
                return pathGet(path)
            end
            function State.onChanged(callback: ChangedListener__DARKLUA_TYPE_c)
                table.insert(listeners, callback)
            end
            function State.fire(path: string, value: any)
                for _, callback in listeners do
                    task.spawn(callback, path, value)
                end
            end
            function State.set(path: string, value: any)
                pathSet(path, value)
                State.fire(path, value)
            end
            function State.setSilent(path: string, value: any)
                pathSet(path, value)
            end

            local function deepcopy(value: any): any
                if typeof(value) ~= 'table' then
                    return value
                end

                local copy = {}

                for key, v in value do
                    copy[key] = deepcopy(v)
                end

                return copy
            end

            function State.snapshot(): Config__DARKLUA_TYPE_g
                return deepcopy(Config)
            end
            function State.applySnapshot(snapshot: Config__DARKLUA_TYPE_g)
                for path, control in Controls do
                    local node: any = snapshot

                    for key in string.gmatch(path, '[^%.]+')do
                        if typeof(node) ~= 'table' then
                            node = nil

                            break
                        end

                        node = node[key]
                    end

                    if node ~= nil then
                        control.Set(node)
                    end
                end
            end

            return State
        end

        function __DARKLUA_BUNDLE_MODULES.c(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.c

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.c = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local TweenService = game:GetService('TweenService')
            local Theme = {}

            local function montserrat(weight: Enum.FontWeight): Font
                return Font.new('rbxasset://fonts/families/Montserrat.json', weight, Enum.FontStyle.Normal)
            end

            Theme.Font = {
                Medium = montserrat(Enum.FontWeight.Medium),
                SemiBold = montserrat(Enum.FontWeight.SemiBold),
                Bold = montserrat(Enum.FontWeight.Bold),
            }

            local GRAY = {
                Accent = Color3.fromRGB(124, 108, 246),
                Text = Color3.fromRGB(242, 242, 246),
                Muted = Color3.fromRGB(142, 142, 153),
                Background = Color3.fromRGB(24, 25, 30),
                Panel = Color3.fromRGB(29, 30, 37),
                Popup = Color3.fromRGB(29, 30, 37),
                Slot = Color3.fromRGB(29, 30, 36),
                Highlight = Color3.fromRGB(38, 38, 46),
                Divider = Color3.fromRGB(38, 38, 46),
                Field = Color3.fromRGB(41, 43, 54),
                DropdownButton = Color3.fromRGB(35, 35, 44),
                PopupBackground = Color3.fromRGB(23, 23, 30),
                OptionRow = Color3.fromRGB(36, 36, 45),
                PickerBackground = Color3.fromRGB(28, 28, 36),
                PresetField = Color3.fromRGB(31, 32, 39),
                PresetRow = Color3.fromRGB(33, 34, 42),
                Toggle = Color3.fromRGB(36, 36, 46),
                KnobOff = Color3.fromRGB(63, 63, 75),
                Track = Color3.fromRGB(52, 52, 62),
            }
            local BLACK = {
                Accent = Color3.fromRGB(124, 108, 246),
                Text = Color3.fromRGB(242, 242, 246),
                Muted = Color3.fromRGB(142, 142, 153),
                Background = Color3.fromRGB(9, 10, 15),
                Panel = Color3.fromRGB(13, 14, 19),
                Popup = Color3.fromRGB(9, 10, 15),
                Slot = Color3.fromRGB(22, 23, 31),
                Highlight = Color3.fromRGB(22, 23, 31),
                Divider = Color3.fromRGB(38, 38, 46),
                Field = Color3.fromRGB(22, 23, 31),
                DropdownButton = Color3.fromRGB(26, 28, 37),
                PopupBackground = Color3.fromRGB(9, 10, 15),
                OptionRow = Color3.fromRGB(22, 23, 31),
                PickerBackground = Color3.fromRGB(13, 14, 19),
                PresetField = Color3.fromRGB(24, 23, 42),
                PresetRow = Color3.fromRGB(24, 23, 42),
                Toggle = Color3.fromRGB(36, 36, 46),
                KnobOff = Color3.fromRGB(63, 63, 75),
                Track = Color3.fromRGB(52, 52, 62),
            }

            Theme.Palettes = {
                Gray = GRAY,
                Black = BLACK,
            }

            local live: {[string]: Color3} = {}

            for token, color in GRAY do
                live[token] = color
            end

            Theme.Color = live
            Theme.Style = 'Gray'

            local bindings: {Binding__DARKLUA_TYPE_m} = {}
            local APPLY_TWEEN = TweenInfo.new(0.3, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)

            function Theme.bind(
                instance: Instance,
                property: string,
                token: string
            )
                local target = instance::any

                target[property] = Theme.Color[token]

                table.insert(bindings, {
                    instance = instance,
                    property = property,
                    token = token,
                })
            end
            function Theme.setStyle(style: string)
                local palette = Theme.Palettes[style]

                if not palette then
                    return
                end

                Theme.Style = style

                for token, color in palette do
                    Theme.Color[token] = color
                end
                for _, binding in bindings do
                    pcall(function()
                        TweenService:Create(binding.instance, APPLY_TWEEN, {
                            [binding.property] = palette[binding.token],
                        }):Play()
                    end)
                end
            end

            return Theme
        end

        function __DARKLUA_BUNDLE_MODULES.d(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.d

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.d = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local UI = {}

            function UI.new(class: string, props: {[string]: any}): Instance
                local ok, inst = pcall(Instance.new, class)

                if not ok then
                    local fallback = Instance.new('Folder')

                    fallback.Name = class .. '_Unsupported'

                    if props.Parent then
                        fallback.Parent = props.Parent
                    end

                    return fallback
                end

                local parent: Instance? = nil

                for key, value in props do
                    if key == 'Parent' then
                        parent = value
                    else
                        (inst::any)[key] = value
                    end
                end

                if parent then
                    inst.Parent = parent
                end

                return inst
            end
            function UI.corner(parent: Instance, radius: number?): UICorner
                return UI.new('UICorner', {
                    CornerRadius = UDim.new(0, radius or 8),
                    Parent = parent,
                })::UICorner
            end
            function UI.stroke(
                parent: Instance,
                color: Color3,
                transparency: number?,
                thickness: number?
            ): UIStroke
                return UI.new('UIStroke', {
                    Color = color,
                    Transparency = transparency or 0,
                    Thickness = thickness or 1,
                    Parent = parent,
                })::UIStroke
            end
            function UI.hitButton(
                parent: GuiObject,
                size: UDim2?,
                position: UDim2?,
                zIndex: number?
            ): TextButton
                return UI.new('TextButton', {
                    Name = 'Hit',
                    BackgroundTransparency = 1,
                    Text = '',
                    AutoButtonColor = false,
                    Size = size or UDim2.new(1, 0, 1, 0),
                    Position = position or UDim2.new(),
                    ZIndex = zIndex or (parent.ZIndex + 2),
                    Parent = parent,
                })::TextButton
            end

            return UI
        end

        function __DARKLUA_BUNDLE_MODULES.e(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.e

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.e = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local Theme = __DARKLUA_BUNDLE_MODULES.d()
            local UI = __DARKLUA_BUNDLE_MODULES.e()
            local Watermark = {}

            function Watermark.create(parent: Instance): WatermarkRefs__DARKLUA_TYPE_n
                local frame = UI.new('Frame', {
                    Name = 'watermark',
                    AnchorPoint = Vector2.new(1, 0),
                    BorderSizePixel = 0,
                    Position = UDim2.new(0.563009, -24, -0.0431566, 110),
                    Size = UDim2.new(0, 208, 0, 48),
                    Parent = parent,
                })::Frame

                Theme.bind(frame, 'BackgroundColor3', 'Background')
                UI.corner(frame, 12)
                UI.stroke(frame, Color3.fromRGB(44, 44, 54), 0.35)
                UI.new('ImageLabel', {
                    Name = 'IMG_PinIcon',
                    BackgroundTransparency = 1,
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 5, 0, 4),
                    Size = UDim2.new(0, 47, 0, 40),
                    Image = 'rbxassetid://130721167799127',
                    ImageColor3 = Color3.fromRGB(236, 236, 241),
                    ScaleType = Enum.ScaleType.Fit,
                    Parent = frame,
                })
                UI.new('Frame', {
                    Name = 'Divider',
                    BackgroundColor3 = Color3.fromRGB(44, 44, 54),
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 52, 0, 12),
                    Size = UDim2.new(0, 1, 0, 24),
                    Parent = frame,
                })

                local fpsLabel = UI.new('TextLabel', {
                    Name = 'FPS',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 70, 0, 0),
                    Size = UDim2.new(0, 64, 0, 48),
                    FontFace = Theme.Font.SemiBold,
                    Text = '144 FPS',
                    TextColor3 = Color3.fromRGB(236, 236, 241),
                    TextSize = 13,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    Parent = frame,
                })::TextLabel
                local pingLabel = UI.new('TextLabel', {
                    Name = 'Ping',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 134, 0, 0),
                    Size = UDim2.new(0, 68, 0, 48),
                    FontFace = Theme.Font.SemiBold,
                    Text = '64 PING',
                    TextColor3 = Color3.fromRGB(236, 236, 241),
                    TextSize = 13,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    Parent = frame,
                })::TextLabel

                return {
                    Frame = frame,
                    FPSLabel = fpsLabel,
                    PingLabel = pingLabel,
                }
            end

            return Watermark
        end

        function __DARKLUA_BUNDLE_MODULES.f(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.f

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.f = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local Theme = __DARKLUA_BUNDLE_MODULES.d()
            local UI = __DARKLUA_BUNDLE_MODULES.e()
            local ROWS = 3
            local COLUMNS = 9
            local CELL = 56
            local SLOT_SIZE = 52
            local ICON_INSET = 5
            local ICON_SIZE = 42
            local ICON_IMAGE = 'rbxassetid://132649014541604'
            local ItemGrid = {}

            function ItemGrid.create(parent: Instance): Frame
                local frame = UI.new('Frame', {
                    Name = 'Item_Grid',
                    AnchorPoint = Vector2.new(1, 1),
                    BorderSizePixel = 0,
                    Position = UDim2.new(1, -24, 1, -20),
                    Size = UDim2.new(0, 516, 0, 180),
                    Parent = parent,
                })::Frame

                Theme.bind(frame, 'BackgroundColor3', 'Background')
                UI.corner(frame, 12)
                UI.new('UIPadding', {
                    PaddingBottom = UDim.new(0, 8),
                    PaddingLeft = UDim.new(0, 8),
                    PaddingRight = UDim.new(0, 8),
                    PaddingTop = UDim.new(0, 8),
                    Parent = frame,
                })

                for row = 1, ROWS do
                    for column = 1, COLUMNS do
                        local slot = UI.new('Frame', {
                            Name = string.format('GridSlot_%d_%d', row, column),
                            BackgroundTransparency = 0.05,
                            BorderSizePixel = 0,
                            Position = UDim2.new(0, (column - 1) * CELL, 0, (row - 1) * CELL),
                            Size = UDim2.new(0, SLOT_SIZE, 0, SLOT_SIZE),
                            Parent = frame,
                        })

                        Theme.bind(slot, 'BackgroundColor3', 'Slot')
                        UI.corner(slot, 10)
                        UI.new('ImageLabel', {
                            Name = 'IMG_Totem',
                            BackgroundTransparency = 1,
                            BorderSizePixel = 0,
                            Position = UDim2.new(0, ICON_INSET, 0, ICON_INSET),
                            Size = UDim2.new(0, ICON_SIZE, 0, ICON_SIZE),
                            Image = ICON_IMAGE,
                            ScaleType = Enum.ScaleType.Fit,
                            Parent = slot,
                        })
                    end
                end

                return frame
            end

            return ItemGrid
        end

        function __DARKLUA_BUNDLE_MODULES.g(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.g

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.g = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local Theme = __DARKLUA_BUNDLE_MODULES.d()
            local UI = __DARKLUA_BUNDLE_MODULES.e()
            local ARMOR_ICONS = {
                'rbxassetid://123317304556076',
                'rbxassetid://110221288961917',
                'rbxassetid://124423750088531',
                'rbxassetid://111380676239074',
            }
            local ArmorHotbar = {}

            function ArmorHotbar.create(parent: Instance): Frame
                local frame = UI.new('Frame', {
                    Name = 'armor_hotbar',
                    AnchorPoint = Vector2.new(0.5, 1),
                    BackgroundColor3 = Color3.fromRGB(28, 28, 36),
                    BackgroundTransparency = 1,
                    BorderSizePixel = 0,
                    Position = UDim2.new(0.439466, 102, 1.00863, -28),
                    Size = UDim2.new(0, 290, 0, 66),
                    Parent = parent,
                })::Frame

                for index, icon in ARMOR_ICONS do
                    local slot = UI.new('Frame', {
                        Name = 'ArmorSlot_' .. index,
                        BackgroundTransparency = 0.12,
                        BorderSizePixel = 0,
                        Position = UDim2.new(0, (index - 1) * 76, 0, 0),
                        Size = UDim2.new(0, 62, 0, 66),
                        Parent = frame,
                    })

                    Theme.bind(slot, 'BackgroundColor3', 'Slot')
                    UI.corner(slot, 10)
                    UI.new('ImageLabel', {
                        Name = 'IMG_Item',
                        BackgroundTransparency = 1,
                        Position = UDim2.new(0, 15, 0, 5),
                        Size = UDim2.new(0, 32, 0, 32),
                        Image = icon,
                        ImageColor3 = Color3.fromRGB(154, 154, 164),
                        ScaleType = Enum.ScaleType.Fit,
                        Parent = slot,
                    })
                    UI.new('TextLabel', {
                        Name = 'Percent',
                        BackgroundTransparency = 1,
                        Position = UDim2.new(0, 0, 0, 38),
                        Size = UDim2.new(0, 62, 0, 16),
                        FontFace = Theme.Font.SemiBold,
                        Text = '100%',
                        TextColor3 = Color3.fromRGB(244, 244, 248),
                        TextSize = 12,
                        Parent = slot,
                    })

                    local bar = UI.new('Frame', {
                        Name = 'DurabilityBar',
                        BackgroundColor3 = Color3.fromRGB(62, 220, 113),
                        BorderSizePixel = 0,
                        Position = UDim2.new(0, 10, 0, 58),
                        Size = UDim2.new(0, 42, 0, 3),
                        Parent = slot,
                    })

                    UI.corner(bar, 2)
                end

                return frame
            end

            return ArmorHotbar
        end

        function __DARKLUA_BUNDLE_MODULES.h(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.h

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.h = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local TweenService = game:GetService('TweenService')
            local FADE_TWEEN = TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
            local VISIBLE_TRANSPARENCY = 0.55
            local FALLBACK_THICKNESS = {4, 3, 2}
            local FALLBACK_TRANSPARENCY = 0.93
            local supportsUIShadow = pcall(function()
                Instance.new('UIShadow'):Destroy()
            end)
            local Shadow = {}

            function Shadow.create(parent: GuiObject): ShadowHandle__DARKLUA_TYPE_o
                local handle = {}::ShadowHandle__DARKLUA_TYPE_o

                if supportsUIShadow then
                    local shadow = Instance.new('UIShadow')

                    pcall(function()
                        (shadow::any).BlurRadius = UDim.new(0, 20)
                    end)
                    pcall(function()
                        (shadow::any).Transparency = 1
                    end)
                    pcall(function()
                        (shadow::any).ZIndex = -1
                    end)

                    shadow.Parent = parent

                    function handle:Render(
                        visible: boolean,
                        strength: number?
                    )
                        local target = if visible then(strength or VISIBLE_TRANSPARENCY)else 1

                        pcall(function()
                            TweenService:Create(shadow, FADE_TWEEN, {Transparency = target}):Play()
                        end)
                    end
                else
                    local strokes: {UIStroke} = {}

                    for _, thickness in FALLBACK_THICKNESS do
                        local stroke = Instance.new('UIStroke')

                        stroke.Thickness = thickness
                        stroke.Color = Color3.new(0, 0, 0)
                        stroke.Transparency = 1
                        stroke.Parent = parent

                        table.insert(strokes, stroke)
                    end

                    function handle:Render(
                        visible: boolean,
                        strength: number?
                    )
                        local target = 1

                        if visible then
                            target = math.clamp(FALLBACK_TRANSPARENCY + ((strength or VISIBLE_TRANSPARENCY) - VISIBLE_TRANSPARENCY), 0.8, 0.98)
                        end

                        for _, stroke in strokes do
                            TweenService:Create(stroke, FADE_TWEEN, {Transparency = target}):Play()
                        end
                    end
                end

                return handle
            end

            return Shadow
        end

        function __DARKLUA_BUNDLE_MODULES.i(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.i

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.i = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local TweenService = game:GetService('TweenService')
            local Animate = {}

            Animate.Fast = TweenInfo.new(0.05, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
            Animate.Quick = TweenInfo.new(0.1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
            Animate.Info = TweenInfo.new(0.14, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
            Animate.Smooth = TweenInfo.new(0.175, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
            Animate.Settle = TweenInfo.new(0.22, Enum.EasingStyle.Back, Enum.EasingDirection.Out)
            Animate.Prime = TweenInfo.new(0.25, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
            Animate.Pill = TweenInfo.new(0.25, Enum.EasingStyle.Quart, Enum.EasingDirection.Out)
            Animate.Glide = TweenInfo.new(0.28, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
            Animate.Slow = TweenInfo.new(0.35, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
            Animate.Bounce = TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out)

            function Animate.tween(
                target: Instance,
                props: {[string]: any},
                info: TweenInfo?
            ): Tween
                local tween = TweenService:Create(target, info or Animate.Info, props)

                tween:Play()

                return tween
            end

            return Animate
        end

        function __DARKLUA_BUNDLE_MODULES.j(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.j

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.j = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local Players = game:GetService('Players')
            local Theme = __DARKLUA_BUNDLE_MODULES.d()
            local UI = __DARKLUA_BUNDLE_MODULES.e()
            local Shadow = __DARKLUA_BUNDLE_MODULES.i()
            local Animate = __DARKLUA_BUNDLE_MODULES.j()
            local LocalPlayer = Players.LocalPlayer
            local MainWindow = {}

            function MainWindow.create(parent: Instance): MainWindowRefs__DARKLUA_TYPE_p
                local window = UI.new('Frame', {
                    Name = 'Main_Menu_Window',
                    AnchorPoint = Vector2.new(0.5, 0.5),
                    BorderSizePixel = 0,
                    Position = UDim2.new(0.453216, -10, 0.437115, 18),
                    Size = UDim2.new(0, 718, 0, 440),
                    Parent = parent,
                })::Frame

                Theme.bind(window, 'BackgroundColor3', 'Background')
                UI.corner(window, 14)

                local scale = UI.new('UIScale', {Parent = window})::UIScale
                local leftPanel = UI.new('Frame', {
                    Name = 'left_panel',
                    BackgroundTransparency = 0.04,
                    BorderSizePixel = 0,
                    Position = UDim2.new(-0.0375464, 0, 0.0181818, 0),
                    Size = UDim2.new(0, 180, 0, 424),
                    Parent = window,
                })::Frame

                Theme.bind(leftPanel, 'BackgroundColor3', 'Panel')
                UI.corner(leftPanel, 14)
                Shadow.create(leftPanel):Render(true)

                local logo = UI.new('ImageLabel', {
                    Name = 'IMG_Logo',
                    BackgroundTransparency = 1,
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 18, 0, 14),
                    Size = UDim2.new(0, 144, 0, 36),
                    Image = 'rbxassetid://115755065995940',
                    ScaleType = Enum.ScaleType.Fit,
                    Parent = leftPanel,
                })::ImageLabel
                local userCard = UI.new('Frame', {
                    Name = 'UserCard',
                    BackgroundColor3 = Color3.fromRGB(20, 20, 25),
                    BackgroundTransparency = 1,
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 0, 0, 380),
                    Size = UDim2.new(0, 158, 0, 32),
                    Parent = leftPanel,
                })::Frame

                UI.new('TextLabel', {
                    Name = 'Name',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 57, 0, 0),
                    Size = UDim2.new(0, 90, 0, 13),
                    FontFace = Theme.Font.SemiBold,
                    Text = if LocalPlayer then LocalPlayer.Name else'Player',
                    TextColor3 = Theme.Color.Text,
                    TextSize = 12,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    TextTruncate = Enum.TextTruncate.AtEnd,
                    Parent = userCard,
                })
                UI.new('TextLabel', {
                    Name = 'Expiry',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 57, 0, 15),
                    Size = UDim2.new(0, 96, 0, 11),
                    FontFace = Theme.Font.SemiBold,
                    Text = 'Till:  Lifetime',
                    TextColor3 = Theme.Color.Muted,
                    TextSize = 10,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    Parent = userCard,
                })

                local avatar = UI.new('ImageLabel', {
                    Name = 'IMG_Avatar',
                    BackgroundColor3 = Color3.fromRGB(255, 255, 255),
                    BackgroundTransparency = 1,
                    BorderSizePixel = 0,
                    Position = UDim2.new(0.0949367, 0, 0.0198879, 0),
                    Size = UDim2.new(0, 32, 0, 30),
                    Image = if LocalPlayer then string.format('rbxthumb://type=AvatarHeadShot&id=%d&w=48&h=48', LocalPlayer.UserId)else'',
                    Parent = userCard,
                })::ImageLabel
                local avatarCorner = UI.corner(avatar)

                avatarCorner.CornerRadius = UDim.new(1, 0)

                local topBar = UI.new('Frame', {
                    Name = 'top_bar_content',
                    BackgroundColor3 = Color3.fromRGB(26, 26, 33),
                    BackgroundTransparency = 1,
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 158, 0, 0),
                    Size = UDim2.new(0, 562, 0, 62),
                    Parent = window,
                })::Frame
                local search = UI.new('Frame', {
                    Name = 'search',
                    BackgroundTransparency = 0.1,
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 8, 0, 23),
                    Size = UDim2.new(0, 207, 0, 34),
                    Parent = topBar,
                })::Frame

                Theme.bind(search, 'BackgroundColor3', 'Panel')
                UI.corner(search, 10)
                Shadow.create(search):Render(true)
                UI.new('ImageLabel', {
                    Name = 'IMG_SearchIcon',
                    BackgroundTransparency = 1,
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 12, 0, 10),
                    Size = UDim2.new(0, 14, 0, 14),
                    Image = 'rbxassetid://109659294248163',
                    ScaleType = Enum.ScaleType.Fit,
                    Parent = search,
                })

                local searchBox = UI.new('TextBox', {
                    Name = 'Input',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 36, 0, 0),
                    Size = UDim2.new(0, 160, 0, 34),
                    FontFace = Theme.Font.SemiBold,
                    Text = '',
                    PlaceholderText = 'Search elements',
                    PlaceholderColor3 = Theme.Color.Muted,
                    TextColor3 = Theme.Color.Text,
                    TextSize = 12,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    ClearTextOnFocus = false,
                    Parent = search,
                })::TextBox
                local searchRing = UI.stroke(search, Theme.Color.Accent, 1, 1)

                Theme.bind(searchRing, 'Color', 'Accent')
                searchBox.Focused:Connect(function()
                    Animate.tween(searchRing, {Transparency = 0.4}, Animate.Quick)
                end)
                searchBox.FocusLost:Connect(function()
                    Animate.tween(searchRing, {Transparency = 1}, Animate.Quick)
                end)

                local presetButton = UI.new('Frame', {
                    Name = 'LegitPvP_Button',
                    BackgroundTransparency = 0.1,
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 453, 0, 23),
                    Size = UDim2.new(0, 95, 0, 34),
                    Parent = topBar,
                })::Frame

                Theme.bind(presetButton, 'BackgroundColor3', 'Panel')
                UI.corner(presetButton, 10)

                local presetButtonLabel = UI.new('TextLabel', {
                    Name = 'Label',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 12, 0, 0),
                    Size = UDim2.new(0, 50, 0, 34),
                    FontFace = Theme.Font.SemiBold,
                    Text = 'Legit PvP',
                    TextColor3 = Theme.Color.Text,
                    TextSize = 12,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    TextTruncate = Enum.TextTruncate.AtEnd,
                    Parent = presetButton,
                })::TextLabel

                UI.new('ImageLabel', {
                    Name = 'IMG_FolderIcon',
                    BackgroundTransparency = 1,
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 70, 0, 9),
                    Size = UDim2.new(0, 16, 0, 16),
                    Image = 'rbxassetid://10723386277',
                    ScaleType = Enum.ScaleType.Fit,
                    Parent = presetButton,
                })

                local breadcrumb = UI.new('Frame', {
                    Name = 'breadcrumb',
                    BackgroundColor3 = Color3.fromRGB(26, 26, 33),
                    BackgroundTransparency = 1,
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 158, 0, 62),
                    Size = UDim2.new(0, 562, 0, 22),
                    Parent = window,
                })::Frame

                UI.new('ImageLabel', {
                    Name = 'IMG_BreadcrumbIcon',
                    BackgroundTransparency = 1,
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 8, 0, 3),
                    Size = UDim2.new(0, 15, 0, 15),
                    Image = 'rbxassetid://10734977012',
                    ScaleType = Enum.ScaleType.Fit,
                    Parent = breadcrumb,
                })

                local crumbSection = UI.new('TextLabel', {
                    Name = 'Section',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 30, 0, 0),
                    Size = UDim2.new(0, 48, 0, 22),
                    FontFace = Theme.Font.SemiBold,
                    Text = 'Combat',
                    TextColor3 = Theme.Color.Muted,
                    TextSize = 12,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    Parent = breadcrumb,
                })::TextLabel
                local crumbArrow = UI.new('TextLabel', {
                    Name = 'Arrow',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 71, 0, -4),
                    Size = UDim2.new(0, 25, 0, 26),
                    FontFace = Theme.Font.SemiBold,
                    Text = '',
                    TextColor3 = Color3.fromRGB(105, 105, 116),
                    TextSize = 18,
                    Parent = breadcrumb,
                })::TextLabel
                local arrowIcon = UI.new('ImageLabel', {
                    BackgroundColor3 = Color3.fromRGB(255, 255, 255),
                    BackgroundTransparency = 1,
                    BorderSizePixel = 0,
                    Position = UDim2.new(0.28, 0, 0.307692, 0),
                    Size = UDim2.new(0, 13, 0, 13),
                    Image = 'rbxassetid://10709768347',
                    Parent = crumbArrow,
                })

                UI.new('UIAspectRatioConstraint', {Parent = arrowIcon})

                local crumbElement = UI.new('TextLabel', {
                    Name = 'Element',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 96, 0, 0),
                    Size = UDim2.new(0, 90, 0, 22),
                    FontFace = Theme.Font.SemiBold,
                    Text = 'AutoTotem',
                    TextColor3 = Color3.fromRGB(206, 206, 215),
                    TextSize = 12,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    Parent = breadcrumb,
                })::TextLabel
                local mainTitle = UI.new('TextLabel', {
                    Name = 'MainTitle',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 166, 0, 92),
                    Size = UDim2.new(0, 200, 0, 16),
                    FontFace = Theme.Font.SemiBold,
                    Text = 'AutoTotem main',
                    TextColor3 = Theme.Color.Text,
                    TextSize = 13,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    Parent = window,
                })::TextLabel
                local extraTitle = UI.new('TextLabel', {
                    Name = 'ExtraTitle',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 444, 0, 92),
                    Size = UDim2.new(0, 200, 0, 16),
                    FontFace = Theme.Font.SemiBold,
                    Text = 'AutoTotem extra',
                    TextColor3 = Theme.Color.Text,
                    TextSize = 13,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    Parent = window,
                })::TextLabel

                return {
                    Window = window,
                    Scale = scale,
                    LeftPanel = leftPanel,
                    Logo = logo,
                    TopBar = topBar,
                    SearchBox = searchBox,
                    PresetButton = presetButton,
                    PresetButtonLabel = presetButtonLabel,
                    CrumbSection = crumbSection,
                    CrumbElement = crumbElement,
                    MainTitle = mainTitle,
                    ExtraTitle = extraTitle,
                }
            end

            return MainWindow
        end

        function __DARKLUA_BUNDLE_MODULES.k(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.k

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.k = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local Animate = __DARKLUA_BUNDLE_MODULES.j()
            local TabIndicator = {}

            function TabIndicator.attach(
                pill: GuiObject,
                container: GuiObject,
                options: Options__DARKLUA_TYPE_r?
            ): TabIndicatorController__DARKLUA_TYPE_s
                local opts: Options__DARKLUA_TYPE_r = options or {}
                local tweenInfo = opts.tweenInfo or Animate.Pill
                local horizontal: HorizontalMode__DARKLUA_TYPE_q = opts.horizontal or 'button'
                local left = opts.left or 0
                local right = opts.right or 0
                local paddingY = opts.paddingY or 0
                local fixedX = pill.Position.X.Offset
                local fixedWidth = pill.Size.X.Offset
                local target: GuiObject? = nil
                local targetConns: {RBXScriptConnection} = {}
                local activeTween: Tween? = nil
                local lastPosition: UDim2? = nil
                local lastSize: UDim2? = nil

                local function layoutScale(): number
                    local sizeX = container.Size.X

                    if sizeX.Scale == 0 and sizeX.Offset > 0 and container.AbsoluteSize.X > 0 then
                        return container.AbsoluteSize.X / sizeX.Offset
                    end

                    return 1
                end
                local function computeGoal(): (UDim2?,UDim2?)
                    local button = target

                    if not button then
                        return nil, nil
                    end

                    local scale = layoutScale()
                    local y = math.round((button.AbsolutePosition.Y - container.AbsolutePosition.Y) / scale) + paddingY
                    local height = math.round(button.AbsoluteSize.Y / scale) - paddingY * 2

                    if horizontal == 'container' then
                        return UDim2.new(0, left, 0, y), UDim2.new(1, -(left + right), 0, height)
                    elseif horizontal == 'fixed' then
                        return UDim2.new(0, fixedX, 0, y), UDim2.new(0, fixedWidth, 0, height)
                    end

                    local x = math.round((button.AbsolutePosition.X - container.AbsolutePosition.X) / scale) + left
                    local width = math.round(button.AbsoluteSize.X / scale) - left - right

                    return UDim2.new(0, x, 0, y), UDim2.new(0, width, 0, height)
                end
                local function apply(
                    position: UDim2,
                    size: UDim2,
                    instant: boolean
                )
                    if activeTween then
                        activeTween:Cancel()

                        activeTween = nil
                    end

                    lastPosition = position
                    lastSize = size

                    if instant then
                        pill.Position = position
                        pill.Size = size

                        return
                    end

                    local tween = Animate.tween(pill, {
                        Position = position,
                        Size = size,
                    }, tweenInfo)

                    activeTween = tween

                    tween.Completed:Once(function()
                        if activeTween == tween then
                            activeTween = nil
                        end
                    end)
                end
                local function onLayoutChanged()
                    if not lastPosition then
                        return
                    end

                    local position, size = computeGoal()

                    if not position or not size then
                        return
                    end
                    if position == lastPosition and size == lastSize then
                        return
                    end

                    apply(position, size, activeTween == nil)
                end

                local containerConns = {
                    container:GetPropertyChangedSignal('AbsolutePosition'):Connect(onLayoutChanged),
                    container:GetPropertyChangedSignal('AbsoluteSize'):Connect(onLayoutChanged),
                }
                local controller = {}::TabIndicatorController__DARKLUA_TYPE_s

                function controller.moveTo(
                    _self,
                    targetButton: GuiObject,
                    instant: boolean?
                )
                    if target ~= targetButton then
                        for _, conn in targetConns do
                            conn:Disconnect()
                        end

                        table.clear(targetConns)

                        target = targetButton

                        table.insert(targetConns, targetButton:GetPropertyChangedSignal('AbsolutePosition'):Connect(onLayoutChanged))
                        table.insert(targetConns, targetButton:GetPropertyChangedSignal('AbsoluteSize'):Connect(onLayoutChanged))
                    end

                    local position, size = computeGoal()

                    if not position or not size then
                        return
                    end

                    apply(position, size, instant == true or lastPosition == nil)
                end
                function controller.refresh(_self)
                    onLayoutChanged()
                end
                function controller.destroy(_self)
                    if activeTween then
                        activeTween:Cancel()

                        activeTween = nil
                    end

                    for _, conn in targetConns do
                        conn:Disconnect()
                    end

                    table.clear(targetConns)

                    for _, conn in containerConns do
                        conn:Disconnect()
                    end

                    table.clear(containerConns)

                    target = nil
                end

                return controller
            end

            return TabIndicator
        end

        function __DARKLUA_BUNDLE_MODULES.l(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.l

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.l = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local Theme = __DARKLUA_BUNDLE_MODULES.d()
            local UI = __DARKLUA_BUNDLE_MODULES.e()
            local Shadow = __DARKLUA_BUNDLE_MODULES.i()
            local Animate = __DARKLUA_BUNDLE_MODULES.j()
            local TabIndicator = __DARKLUA_BUNDLE_MODULES.l()
            local Types = __DARKLUA_BUNDLE_MODULES.b()
            local ARROW_TWEEN = TweenInfo.new(0.22, Enum.EasingStyle.Quart, Enum.EasingDirection.Out)
            local PILL_TWEEN = TweenInfo.new(0.25, Enum.EasingStyle.Quart, Enum.EasingDirection.Out, 0, false, 0.055)
            local TEXT_TWEEN = TweenInfo.new(0.16, Enum.EasingStyle.Quad, Enum.EasingDirection.Out, 0, false, 0.1)
            local NAV_DEFS: {NavDefinition__DARKLUA_TYPE_j} = {
                {
                    name = 'Combat',
                    icon = 'rbxassetid://83752373575368',
                    iconHeight = 13,
                },
                {
                    name = 'Movement',
                    icon = 'rbxassetid://80792036653047',
                    iconHeight = 14,
                },
                {
                    name = 'Visuals',
                    icon = 'rbxassetid://127234874352422',
                    iconHeight = 14,
                },
                {
                    name = 'Render',
                    icon = 'rbxassetid://117371753006597',
                    iconHeight = 14,
                },
                {
                    name = 'Misc',
                    icon = 'rbxassetid://101699539545687',
                    iconHeight = 14,
                },
                {
                    name = 'Theme',
                    icon = 'rbxassetid://127369887384101',
                    iconHeight = 14,
                },
                {
                    name = 'Auto-Buy',
                    icon = 'rbxassetid://79435149356304',
                    iconHeight = 13,
                },
            }
            local Nav = {}

            function Nav.create(
                parent: Instance,
                onPrimaryClick: (tab:string) -> (),
                onSecondaryClick: (tab:string, navFrame:Frame) -> ()
            ): NavRefs__DARKLUA_TYPE_t
                local highlight = UI.new('Frame', {
                    Name = 'Highlight',
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 8, 0, 66),
                    Size = UDim2.new(0, 154, 0, 23),
                    Parent = parent,
                })::Frame

                Theme.bind(highlight, 'BackgroundColor3', 'Highlight')
                UI.corner(highlight, 8)
                Shadow.create(highlight):Render(true)

                local indicator = UI.new('ImageLabel', {
                    Name = 'indicator',
                    BackgroundTransparency = 1,
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 0, 0, 70),
                    Size = UDim2.new(0, 3, 0, 15),
                    Image = 'rbxassetid://90362801488029',
                    Parent = parent,
                })::ImageLabel
                local pill = TabIndicator.attach(highlight, parent::GuiObject, {
                    horizontal = 'container',
                    left = 8,
                    right = 18,
                    paddingY = 1,
                    tweenInfo = PILL_TWEEN,
                })
                local arrow = TabIndicator.attach(indicator, parent::GuiObject, {
                    horizontal = 'fixed',
                    paddingY = 5,
                    tweenInfo = ARROW_TWEEN,
                })
                local items: {[string]: NavItemRefs__DARKLUA_TYPE_l} = {}
                local order: {string} = {}

                for index, def in NAV_DEFS do
                    local navFrame = UI.new('Frame', {
                        Name = 'Nav_' .. def.name:gsub('%-', '_'),
                        BackgroundColor3 = Color3.fromRGB(20, 20, 25),
                        BackgroundTransparency = 1,
                        BorderSizePixel = 0,
                        Position = UDim2.new(0, 0, 0, 65 + (index - 1) * 25),
                        Size = UDim2.new(0, 158, 0, 25),
                        Parent = parent,
                    })::Frame
                    local icon = UI.new('ImageLabel', {
                        Name = 'IMG_Icon',
                        BackgroundTransparency = 1,
                        BorderSizePixel = 0,
                        Position = UDim2.new(0, 22, 0, 6),
                        Size = UDim2.new(0, 14, 0, def.iconHeight),
                        Image = def.icon,
                        ImageColor3 = Theme.Color.Muted,
                        ScaleType = Enum.ScaleType.Fit,
                        ZIndex = 2,
                        Parent = navFrame,
                    })::ImageLabel
                    local label = UI.new('TextLabel', {
                        Name = 'Label',
                        BackgroundTransparency = 1,
                        Position = UDim2.new(0, 43, 0, 0),
                        Size = UDim2.new(0, 100, 0, 26),
                        ZIndex = 2,
                        FontFace = Theme.Font.SemiBold,
                        Text = def.name,
                        TextColor3 = Theme.Color.Muted,
                        TextSize = 12,
                        TextXAlignment = Enum.TextXAlignment.Left,
                        Parent = navFrame,
                    })::TextLabel

                    items[def.name] = {
                        frame = navFrame,
                        icon = icon,
                        label = label,
                    }

                    table.insert(order, def.name)

                    local hit = UI.hitButton(navFrame, nil, nil, 3)

                    hit.MouseEnter:Connect(function()
                        Animate.tween(navFrame, {BackgroundTransparency = 0.92}, Animate.Quick)
                    end)
                    hit.MouseLeave:Connect(function()
                        Animate.tween(navFrame, {BackgroundTransparency = 1}, Animate.Quick)
                    end)
                    hit.MouseButton1Click:Connect(function()
                        onPrimaryClick(def.name)
                    end)
                    hit.MouseButton2Click:Connect(function()
                        onSecondaryClick(def.name, navFrame)
                    end)
                end

                return {
                    items = items,
                    highlight = highlight,
                    indicator = indicator,
                    pill = pill,
                    arrow = arrow,
                    order = order,
                }
            end
            function Nav.select(refs: NavRefs__DARKLUA_TYPE_t, tab: string)
                local entry = refs.items[tab]

                if not entry then
                    return
                end

                refs.arrow:moveTo(entry.frame)
                refs.pill:moveTo(entry.frame)

                for name, item in refs.items do
                    local selected = name == tab

                    Animate.tween(item.icon, {
                        ImageColor3 = selected and Color3.fromRGB(236, 236, 241) or Theme.Color.Muted,
                    }, TEXT_TWEEN)
                    Animate.tween(item.label, {
                        TextColor3 = selected and Theme.Color.Text or Theme.Color.Muted,
                    }, TEXT_TWEEN)
                end
            end

            return Nav
        end

        function __DARKLUA_BUNDLE_MODULES.m(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.m

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.m = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local TweenService = game:GetService('TweenService')
            local INFO = TweenInfo.new(0.25, Enum.EasingStyle.Quint, Enum.EasingDirection.Out)
            local SLIDE_OFFSET = 8
            local HIDE_DELAY = 0.26
            local Fade = {}

            local function transparencyProps(obj: Instance): {string}?
                if obj:IsA('UIStroke') then
                    return {
                        'Transparency',
                    }
                elseif obj:IsA('TextBox') or obj:IsA('TextLabel') or obj:IsA('TextButton') then
                    return {
                        'TextTransparency',
                        'BackgroundTransparency',
                    }
                elseif obj:IsA('ImageLabel') or obj:IsA('ImageButton') then
                    return {
                        'ImageTransparency',
                        'BackgroundTransparency',
                    }
                elseif obj:IsA('ScrollingFrame') then
                    return {
                        'BackgroundTransparency',
                        'ScrollBarImageTransparency',
                    }
                elseif obj:IsA('Frame') then
                    return {
                        'BackgroundTransparency',
                    }
                end

                return nil
            end
            local function originalOf(obj: Instance, prop: string): number
                local key = 'fadeOriginal_' .. prop
                local value = obj:GetAttribute(key)

                if value == nil then
                    local source = obj::any

                    value = source[prop]

                    obj:SetAttribute(key, value)
                end

                return value::number
            end
            local function collect(root: GuiObject): {Instance}
                local objects = root:GetDescendants()

                table.insert(objects, root)

                return objects
            end

            function Fade.show(root: GuiObject)
                for _, obj in collect(root)do
                    local props = transparencyProps(obj)

                    if props then
                        for _, prop in props do
                            local original = originalOf(obj, prop)
                            local target = obj::any

                            target[prop] = 1

                            TweenService:Create(obj, INFO, {[prop] = original}):Play()
                        end
                    end
                end
            end
            function Fade.hide(root: GuiObject)
                for _, obj in collect(root)do
                    local props = transparencyProps(obj)

                    if props then
                        for _, prop in props do
                            originalOf(obj, prop)
                            TweenService:Create(obj, INFO, {[prop] = 1}):Play()
                        end
                    end
                end
            end
            function Fade.popupIn(window: GuiObject, targetPosition: UDim2)
                local token = (window:GetAttribute('fadeToken')::number? or 0) + 1

                window:SetAttribute('fadeToken', token)

                window.Position = targetPosition + UDim2.fromOffset(0, SLIDE_OFFSET)
                window.Visible = true

                Fade.show(window)
                TweenService:Create(window, INFO, {Position = targetPosition}):Play()
            end
            function Fade.stagger(items: {GuiObject}, step: number?)
                local perStep = step or 0.025

                for index, item in items do
                    local info = TweenInfo.new(0.18, Enum.EasingStyle.Quad, Enum.EasingDirection.Out, 0, false, (index - 1) * perStep)
                    local basePosition = item.Position

                    item.Position = basePosition + UDim2.fromOffset(0, 5)

                    TweenService:Create(item, info, {Position = basePosition}):Play()

                    for _, obj in collect(item)do
                        local props = transparencyProps(obj)

                        if props then
                            for _, prop in props do
                                local original = originalOf(obj, prop)
                                local target = obj::any

                                target[prop] = 1

                                TweenService:Create(obj, info, {[prop] = original}):Play()
                            end
                        end
                    end
                end
            end
            function Fade.popupOut(window: GuiObject)
                if not window.Visible then
                    return
                end

                local token = (window:GetAttribute('fadeToken')::number? or 0) + 1

                window:SetAttribute('fadeToken', token)
                Fade.hide(window)
                TweenService:Create(window, INFO, {
                    Position = window.Position + UDim2.fromOffset(0, SLIDE_OFFSET),
                }):Play()
                task.delay(HIDE_DELAY, function()
                    if window:GetAttribute('fadeToken') == token then
                        window.Visible = false
                    end
                end)
            end

            return Fade
        end

        function __DARKLUA_BUNDLE_MODULES.n(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.n

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.n = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local Nav = __DARKLUA_BUNDLE_MODULES.m()
            local Animate = __DARKLUA_BUNDLE_MODULES.j()
            local Fade = __DARKLUA_BUNDLE_MODULES.n()
            local Types = __DARKLUA_BUNDLE_MODULES.b()
            local Tabs = {}

            function Tabs.create(config: TabsConfig__DARKLUA_TYPE_v): TabsController__DARKLUA_TYPE_w
                local currentTab = 'Combat'
                local selectedModule: {[string]: string} = {}

                local function panelKey(tab: string, moduleName: string): string
                    return tab .. '.' .. moduleName
                end

                local basePositions: {[Frame]: UDim2} = {}
                local shown: PanelRefs__DARKLUA_TYPE_u? = nil
                local firstShow = true

                local function baseOf(frame: Frame): UDim2
                    local base = basePositions[frame]

                    if base == nil then
                        base = frame.Position
                        basePositions[frame] = base
                    end

                    return base
                end
                local function hideFrame(frame: Frame, animate: boolean)
                    local token = (frame:GetAttribute('pageToken')::number? or 0) + 1

                    frame:SetAttribute('pageToken', token)

                    if not animate then
                        frame.Visible = false

                        return
                    end

                    local base = baseOf(frame)

                    Fade.hide(frame)
                    Animate.tween(frame, {
                        Position = base - UDim2.fromOffset(12, 0),
                    }, Animate.Glide)
                    task.delay(0.26, function()
                        if frame:GetAttribute('pageToken') == token then
                            frame.Visible = false
                            frame.Position = base
                        end
                    end)
                end
                local function showFrame(frame: Frame, animate: boolean)
                    local token = (frame:GetAttribute('pageToken')::number? or 0) + 1

                    frame:SetAttribute('pageToken', token)

                    local base = baseOf(frame)

                    frame.Visible = true

                    if not animate then
                        frame.Position = base

                        return
                    end

                    frame.Position = base + UDim2.fromOffset(16, 0)

                    Fade.show(frame)
                    Animate.tween(frame, {Position = base}, Animate.Glide)
                end
                local function applyModuleView()
                    local mods = config.tabModules[currentTab]
                    local moduleName = selectedModule[currentTab] or (mods and mods[1] and mods[1].name) or '-'

                    selectedModule[currentTab] = moduleName
                    config.crumbElement.Text = moduleName
                    config.mainTitle.Text = moduleName .. ' main'
                    config.extraTitle.Text = moduleName .. ' extra'

                    local nextPanel = config.panels[panelKey(currentTab, moduleName)]

                    if nextPanel == shown and not firstShow then
                        return
                    end

                    local animate = not firstShow

                    firstShow = false

                    for _, panel in config.panels do
                        if panel ~= shown and panel ~= nextPanel then
                            hideFrame(panel.main, false)
                            hideFrame(panel.extra, false)
                        end
                    end

                    if shown and shown ~= nextPanel then
                        hideFrame(shown.main, animate)
                        hideFrame(shown.extra, animate)
                    end
                    if nextPanel then
                        showFrame(nextPanel.main, animate)
                        showFrame(nextPanel.extra, animate)
                    end

                    shown = nextPanel
                end

                local controller = {}::TabsController__DARKLUA_TYPE_w

                function controller.selectTab(_self, tab: string)
                    currentTab = tab

                    Nav.select(config.navRefs, tab)

                    config.crumbSection.Text = tab

                    applyModuleView()

                    if config.onTabSelected then
                        config.onTabSelected(tab)
                    end
                end
                function controller.selectModule(
                    _self,
                    tab: string,
                    moduleName: string
                )
                    selectedModule[tab] = moduleName

                    if currentTab == tab then
                        applyModuleView()
                    end
                    if config.onModuleSelected then
                        config.onModuleSelected(tab, moduleName)
                    end
                end
                function controller.currentTab()
                    return currentTab
                end
                function controller.currentModule(tab: string)
                    return selectedModule[tab]
                end

                controller:selectTab(currentTab)

                return controller
            end

            return Tabs
        end

        function __DARKLUA_BUNDLE_MODULES.o(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.o

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.o = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local Layout = {}

            function Layout.relativeTo(
                guiObject: GuiObject,
                reference: GuiObject
            ): (number,number)
                local a = guiObject.AbsolutePosition
                local r = reference.AbsolutePosition

                return a.X - r.X, a.Y - r.Y
            end

            return Layout
        end

        function __DARKLUA_BUNDLE_MODULES.p(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.p

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.p = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local Theme = __DARKLUA_BUNDLE_MODULES.d()
            local UI = __DARKLUA_BUNDLE_MODULES.e()
            local Layout = __DARKLUA_BUNDLE_MODULES.p()
            local Shadow = __DARKLUA_BUNDLE_MODULES.i()
            local Fade = __DARKLUA_BUNDLE_MODULES.n()
            local Animate = __DARKLUA_BUNDLE_MODULES.j()
            local ROW_HEIGHT = 24
            local WIDTH = 124
            local DropdownPopup = {}

            function DropdownPopup.create(mainWindow: Frame): DropdownPopupApi__DARKLUA_TYPE_x
                local window = UI.new('Frame', {
                    Name = 'dropdown_window',
                    BorderSizePixel = 0,
                    ClipsDescendants = true,
                    Position = UDim2.new(0, 750, 0, 318),
                    Size = UDim2.new(0, WIDTH, 0, 116),
                    ZIndex = 52,
                    Visible = false,
                    Parent = mainWindow,
                })::Frame

                Theme.bind(window, 'BackgroundColor3', 'PopupBackground')
                UI.corner(window, 10)
                UI.stroke(window, Color3.fromRGB(46, 46, 56), 0.5)

                local shadow = Shadow.create(window)
                local openAnchor: GuiObject? = nil

                local function close()
                    openAnchor = nil

                    shadow:Render(false)
                    Fade.popupOut(window)
                end
                local function open(
                    anchor: GuiObject,
                    options: {string},
                    current: string,
                    onPick: (option:string) -> ()
                )
                    if window.Visible and openAnchor == anchor then
                        close()

                        return
                    end

                    openAnchor = anchor

                    for _, child in window:GetChildren()do
                        if child:IsA('Frame') then
                            child:Destroy()
                        end
                    end

                    local rows: {GuiObject} = {}

                    for index, option in options do
                        local selected = option == current
                        local row = UI.new('Frame', {
                            Name = 'Option_' .. option,
                            BackgroundColor3 = Theme.Color.OptionRow,
                            BackgroundTransparency = selected and 0 or 1,
                            BorderSizePixel = 0,
                            Position = UDim2.new(0, 5, 0, 6 + (index - 1) * ROW_HEIGHT),
                            Size = UDim2.new(0, WIDTH - 10, 0, ROW_HEIGHT),
                            ZIndex = 53,
                            Parent = window,
                        })::Frame

                        UI.corner(row, 6)

                        if selected then
                            UI.new('ImageLabel', {
                                Name = 'IMG_Check',
                                AnchorPoint = Vector2.new(0, 0.5),
                                BackgroundTransparency = 1,
                                Position = UDim2.new(0, 8, 0.5, 0),
                                Size = UDim2.new(0, 10, 0, 10),
                                ZIndex = 53,
                                Image = 'rbxassetid://86817768619372',
                                ImageColor3 = Color3.fromRGB(246, 246, 250),
                                ScaleType = Enum.ScaleType.Fit,
                                Parent = row,
                            })
                        end

                        UI.new('TextLabel', {
                            Name = 'Label',
                            BackgroundTransparency = 1,
                            Position = UDim2.new(0, 26, 0, 0),
                            Size = UDim2.new(0, WIDTH - 36, 0, ROW_HEIGHT),
                            ZIndex = 53,
                            FontFace = selected and Theme.Font.SemiBold or Theme.Font.Medium,
                            Text = option,
                            TextColor3 = selected and Color3.fromRGB(246, 246, 250) or Color3.fromRGB(132, 132, 143),
                            TextSize = 11,
                            TextXAlignment = Enum.TextXAlignment.Left,
                            Parent = row,
                        })

                        local hit = UI.hitButton(row, nil, nil, 54)

                        if not selected then
                            hit.MouseEnter:Connect(function()
                                Animate.tween(row, {BackgroundTransparency = 0.6}, Animate.Quick)
                            end)
                            hit.MouseLeave:Connect(function()
                                Animate.tween(row, {BackgroundTransparency = 1}, Animate.Quick)
                            end)
                        end

                        hit.MouseButton1Click:Connect(function()
                            close()
                            onPick(option)
                        end)
                        table.insert(rows, row)
                    end

                    local fullHeight = 12 + #options * ROW_HEIGHT
                    local rx, ry = Layout.relativeTo(anchor, mainWindow)
                    local position = UDim2.new(0, rx + anchor.AbsoluteSize.X - WIDTH, 0, ry + ROW_HEIGHT)

                    window.Size = UDim2.new(0, WIDTH, 0, math.floor(fullHeight * 0.72))

                    Fade.popupIn(window, position)
                    Animate.tween(window, {
                        Size = UDim2.new(0, WIDTH, 0, fullHeight),
                    }, Animate.Prime)
                    Fade.stagger(rows, 0.025)
                    shadow:Render(true)
                end

                return {
                    open = open,
                    close = close,
                }
            end

            return DropdownPopup
        end

        function __DARKLUA_BUNDLE_MODULES.q(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.q

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.q = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local UserInputService = game:GetService('UserInputService')
            local Connections = __DARKLUA_BUNDLE_MODULES.a()
            local Drag = {}

            function Drag.makeDraggable(frame: GuiObject, handle: GuiObject?)
                local dragHandle: GuiObject = handle or frame
                local dragging = false
                local dragInput: InputObject? = nil
                local dragStart = Vector3.zero
                local startPosition = frame.Position

                Connections.bind(dragHandle.InputBegan, function(
                    input: InputObject
                )
                    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                        dragging = true
                        dragStart = input.Position
                        startPosition = frame.Position

                        input.Changed:Connect(function()
                            if input.UserInputState == Enum.UserInputState.End then
                                dragging = false
                            end
                        end)
                    end
                end)
                Connections.bind(dragHandle.InputChanged, function(
                    input: InputObject
                )
                    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
                        dragInput = input
                    end
                end)
                Connections.bind(UserInputService.InputChanged, function(
                    input: InputObject
                )
                    if input == dragInput and dragging then
                        local delta = input.Position - dragStart

                        frame.Position = UDim2.new(startPosition.X.Scale, startPosition.X.Offset + delta.X, startPosition.Y.Scale, startPosition.Y.Offset + delta.Y)
                    end
                end)
            end
            function Drag.attachDrag(
                button: GuiButton,
                onUpdate: (position:Vector3, began:boolean) -> (),
                onEnd: (() -> ())?
            )
                local dragging = false

                Connections.bind(button.InputBegan, function(input: InputObject)
                    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                        dragging = true

                        onUpdate(input.Position, true)
                    end
                end)
                Connections.bind(UserInputService.InputChanged, function(
                    input: InputObject
                )
                    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                        onUpdate(input.Position, false)
                    end
                end)
                Connections.bind(UserInputService.InputEnded, function(
                    input: InputObject
                )
                    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                        if dragging and onEnd then
                            onEnd()
                        end

                        dragging = false
                    end
                end)
            end

            return Drag
        end

        function __DARKLUA_BUNDLE_MODULES.r(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.r

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.r = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local TableUtils = {}

            function TableUtils.round(n: number): number
                return math.floor(n + 0.5)
            end
            function TableUtils.deepcopy(value: any): any
                if typeof(value) ~= 'table' then
                    return value
                end

                local copy = {}

                for key, v in value do
                    copy[key] = TableUtils.deepcopy(v)
                end

                return copy
            end

            return TableUtils
        end

        function __DARKLUA_BUNDLE_MODULES.s(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.s

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.s = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local Theme = __DARKLUA_BUNDLE_MODULES.d()
            local UI = __DARKLUA_BUNDLE_MODULES.e()
            local Layout = __DARKLUA_BUNDLE_MODULES.p()
            local Drag = __DARKLUA_BUNDLE_MODULES.r()
            local Fade = __DARKLUA_BUNDLE_MODULES.n()
            local Animate = __DARKLUA_BUNDLE_MODULES.j()
            local Shadow = __DARKLUA_BUNDLE_MODULES.i()
            local TableUtils = __DARKLUA_BUNDLE_MODULES.s()
            local State = __DARKLUA_BUNDLE_MODULES.c()
            local WIDTH = 150
            local HEIGHT = 178
            local PALETTE_W = 106
            local PALETTE_H = 106
            local ColorPicker = {}

            function ColorPicker.create(mainWindow: Frame): ColorPickerApi__DARKLUA_TYPE_y
                local window = UI.new('Frame', {
                    Name = 'color_edit_window',
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, -180, 0, 60),
                    Size = UDim2.new(0, WIDTH, 0, HEIGHT),
                    ZIndex = 50,
                    Visible = false,
                    Parent = mainWindow,
                })::Frame

                Theme.bind(window, 'BackgroundColor3', 'PickerBackground')
                UI.corner(window, 10)
                UI.stroke(window, Color3.fromRGB(48, 48, 58), 0.45)

                local shadow = Shadow.create(window)
                local palette = UI.new('Frame', {
                    Name = 'Palette',
                    BackgroundColor3 = Color3.fromRGB(0, 110, 255),
                    BorderSizePixel = 0,
                    ClipsDescendants = true,
                    Position = UDim2.new(0, 10, 0, 10),
                    Size = UDim2.new(0, PALETTE_W, 0, PALETTE_H),
                    ZIndex = 51,
                    Parent = window,
                })::Frame

                UI.corner(palette, 6)

                do
                    local white = UI.new('Frame', {
                        Name = 'WhiteOverlay',
                        BackgroundColor3 = Color3.fromRGB(255, 255, 255),
                        BorderSizePixel = 0,
                        Size = UDim2.new(1, 0, 1, 0),
                        ZIndex = 51,
                        Parent = palette,
                    })::Frame

                    UI.new('UIGradient', {
                        Transparency = NumberSequence.new({
                            NumberSequenceKeypoint.new(0, 0),
                            NumberSequenceKeypoint.new(1, 1),
                        }),
                        Parent = white,
                    })
                    UI.corner(white, 6)

                    local black = UI.new('Frame', {
                        Name = 'BlackOverlay',
                        BackgroundColor3 = Color3.fromRGB(0, 0, 0),
                        BorderSizePixel = 0,
                        Size = UDim2.new(1, 0, 1, 0),
                        ZIndex = 51,
                        Parent = palette,
                    })::Frame

                    UI.new('UIGradient', {
                        Rotation = 90,
                        Transparency = NumberSequence.new({
                            NumberSequenceKeypoint.new(0, 1),
                            NumberSequenceKeypoint.new(1, 0),
                        }),
                        Parent = black,
                    })
                    UI.corner(black, 6)
                end

                local selector = UI.new('Frame', {
                    Name = 'Selector',
                    BackgroundTransparency = 1,
                    BorderSizePixel = 0,
                    Position = UDim2.new(0.9, 0, 0, 0),
                    Size = UDim2.new(0, 10, 0, 10),
                    ZIndex = 52,
                    Parent = palette,
                })::Frame

                UI.corner(selector, 4)
                UI.stroke(selector, Color3.fromRGB(255, 255, 255), 0, 2)

                local hueStrip = UI.new('Frame', {
                    Name = 'HueStrip',
                    BackgroundColor3 = Color3.fromRGB(255, 255, 255),
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 124, 0, 10),
                    Size = UDim2.new(0, 12, 0, PALETTE_H),
                    ZIndex = 51,
                    Parent = window,
                })::Frame

                UI.corner(hueStrip, 5)
                UI.new('UIGradient', {
                    Color = ColorSequence.new({
                        ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 0, 0)),
                        ColorSequenceKeypoint.new(0.17, Color3.fromRGB(255, 255, 0)),
                        ColorSequenceKeypoint.new(0.33, Color3.fromRGB(0, 255, 0)),
                        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 255, 255)),
                        ColorSequenceKeypoint.new(0.67, Color3.fromRGB(0, 0, 255)),
                        ColorSequenceKeypoint.new(0.83, Color3.fromRGB(255, 0, 255)),
                        ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 0, 0)),
                    }),
                    Rotation = 90,
                    Parent = hueStrip,
                })

                local hueKnob = UI.new('Frame', {
                    Name = 'Knob',
                    BackgroundColor3 = Color3.fromRGB(255, 255, 255),
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 0, 0, 0),
                    Size = UDim2.new(0, 12, 0, 9),
                    ZIndex = 52,
                    Parent = hueStrip,
                })::Frame

                UI.corner(hueKnob, 4)
                UI.stroke(hueKnob, Color3.fromRGB(30, 30, 38), 0, 2)

                local alphaBar = UI.new('Frame', {
                    Name = 'AlphaBar',
                    BackgroundColor3 = Color3.fromRGB(42, 42, 52),
                    BorderSizePixel = 0,
                    ClipsDescendants = true,
                    Position = UDim2.new(0, 10, 0, 124),
                    Size = UDim2.new(0, 126, 0, 10),
                    ZIndex = 51,
                    Parent = window,
                })::Frame

                UI.corner(alphaBar, 6)
                UI.new('ImageLabel', {
                    Name = 'Checkers',
                    BackgroundTransparency = 1,
                    BorderSizePixel = 0,
                    Image = 'rbxassetid://18274452449',
                    ImageColor3 = Color3.fromRGB(255, 255, 255),
                    ImageTransparency = 0.6,
                    ScaleType = Enum.ScaleType.Tile,
                    TileSize = UDim2.new(0, 6, 0, 6),
                    Size = UDim2.new(1, 0, 1, 0),
                    ZIndex = 51,
                    Parent = alphaBar,
                })

                local ramp = UI.new('Frame', {
                    Name = 'Ramp',
                    BackgroundColor3 = Color3.fromRGB(28, 108, 221),
                    BorderSizePixel = 0,
                    Size = UDim2.new(1, 0, 1, 0),
                    ZIndex = 51,
                    Parent = alphaBar,
                })::Frame

                UI.corner(ramp, 6)
                UI.new('UIGradient', {
                    Transparency = NumberSequence.new({
                        NumberSequenceKeypoint.new(0, 1),
                        NumberSequenceKeypoint.new(1, 0),
                    }),
                    Parent = ramp,
                })

                local alphaKnob = UI.new('Frame', {
                    Name = 'Knob',
                    BackgroundColor3 = Color3.fromRGB(255, 255, 255),
                    BorderSizePixel = 0,
                    Position = UDim2.new(0.93, 0, 0, 0),
                    Size = UDim2.new(0, 9, 0, 10),
                    ZIndex = 52,
                    Parent = alphaBar,
                })::Frame

                UI.corner(alphaKnob, 4)
                UI.stroke(alphaKnob, Color3.fromRGB(30, 30, 38), 0, 2)

                local hexField = UI.new('Frame', {
                    Name = 'HexField',
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 10, 0, 142),
                    Size = UDim2.new(0, 126, 0, 24),
                    ZIndex = 51,
                    Parent = window,
                })::Frame

                Theme.bind(hexField, 'BackgroundColor3', 'Field')
                UI.corner(hexField, 6)

                local hexBox = UI.new('TextBox', {
                    Name = 'Value',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 9, 0, 0),
                    Size = UDim2.new(0, 108, 0, 24),
                    ZIndex = 51,
                    FontFace = Theme.Font.Medium,
                    Text = '1C6CDDFF',
                    TextColor3 = Color3.fromRGB(244, 244, 248),
                    TextSize = 11,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    ClearTextOnFocus = false,
                    Parent = hexField,
                })::TextBox
                local hue, saturation, value, alpha = 0.6, 0.87, 0.87, 1
                local target: {swatch: Frame, path: string}? = nil

                local function currentColor(): Color3
                    return Color3.fromHSV(hue, saturation, value)
                end
                local function currentHex(): string
                    local color = currentColor()

                    return string.format('%02X%02X%02X%02X', TableUtils.round(color.R * 255), TableUtils.round(color.G * 255), TableUtils.round(color.B * 255), TableUtils.round(alpha * 255))
                end
                local function render()
                    local color = currentColor()

                    Animate.tween(palette, {
                        BackgroundColor3 = Color3.fromHSV(hue, 1, 1),
                    }, Animate.Quick)
                    Animate.tween(selector, {
                        Position = UDim2.new(math.min(saturation, 0.92), 0, math.min(1 - value, 0.92), 0),
                    }, Animate.Quick)
                    Animate.tween(hueKnob, {
                        Position = UDim2.new(0, 0, math.min(hue, 0.92), 0),
                    }, Animate.Quick)
                    Animate.tween(alphaKnob, {
                        Position = UDim2.new(math.min(alpha, 0.93), 0, 0, 0),
                    }, Animate.Quick)
                    Animate.tween(ramp, {BackgroundColor3 = color}, Animate.Quick)

                    hexBox.Text = currentHex()

                    if target then
                        Animate.tween(target.swatch, {BackgroundColor3 = color}, Animate.Quick)
                        State.set(target.path, color)
                    end
                end
                local function close()
                    shadow:Render(false)
                    Fade.popupOut(window)
                end
                local function open(
                    swatch: Frame,
                    path: string,
                    _title: string
                )
                    if window.Visible and target and target.swatch == swatch then
                        close()

                        target = nil

                        return
                    end

                    target = {
                        swatch = swatch,
                        path = path,
                    }

                    local color = State.get(path) or swatch.BackgroundColor3

                    hue, saturation, value = color:ToHSV()
                    alpha = 1

                    local rx, ry = Layout.relativeTo(swatch, mainWindow)
                    local position = UDim2.new(0, math.max(-350, rx - WIDTH - 12), 0, math.clamp(ry - 80, 
-120, 240))

                    Fade.popupIn(window, position)
                    shadow:Render(true)
                    render()
                end

                local paletteHit = UI.hitButton(palette, nil, nil, 53)

                Drag.attachDrag(paletteHit, function(position)
                    saturation = math.clamp((position.X - palette.AbsolutePosition.X) / math.max(1, palette.AbsoluteSize.X), 0, 1)
                    value = 1 - math.clamp((position.Y - palette.AbsolutePosition.Y) / math.max(1, palette.AbsoluteSize.Y), 0, 1)

                    render()
                end)

                local hueHit = UI.hitButton(hueStrip, UDim2.new(0, 20, 1, 0), UDim2.new(0, 
-3, 0, 0), 53)

                Drag.attachDrag(hueHit, function(position)
                    hue = math.clamp((position.Y - hueStrip.AbsolutePosition.Y) / math.max(1, hueStrip.AbsoluteSize.Y), 0, 0.999)

                    render()
                end)

                local alphaHit = UI.hitButton(alphaBar, UDim2.new(1, 0, 0, 18), UDim2.new(0, 0, 0, 
-3), 53)

                Drag.attachDrag(alphaHit, function(position)
                    alpha = math.clamp((position.X - alphaBar.AbsolutePosition.X) / math.max(1, alphaBar.AbsoluteSize.X), 0, 1)

                    render()
                end)
                hexBox.FocusLost:Connect(function()
                    local hex = hexBox.Text:gsub('#', ''):upper()
                    local r, g, b, a = hex:match('^(%x%x)(%x%x)(%x%x)(%x?%x?)$')

                    if r then
                        local color = Color3.fromRGB(tonumber(r, 16)::number, tonumber(g, 16)::number, tonumber(b, 16)::number)

                        hue, saturation, value = color:ToHSV()
                        alpha = if a ~= ''then(tonumber(a, 16)::number) / 255 else 1
                    end

                    render()
                end)

                return {
                    open = open,
                    close = close,
                }
            end

            return ColorPicker
        end

        function __DARKLUA_BUNDLE_MODULES.t(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.t

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.t = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local UserInputService = game:GetService('UserInputService')
            local Connections = __DARKLUA_BUNDLE_MODULES.a()
            local MENU_KEY = Enum.KeyCode.RightShift
            local InputManager = {}
            local pendingCapture: PendingCapture__DARKLUA_TYPE_A? = nil
            local keyDownListeners: {(keyCode:Enum.KeyCode) -> ()} = {}
            local keyUpListeners: {(keyCode:Enum.KeyCode) -> ()} = {}
            local menuToggleListeners: {() -> ()} = {}
            local started = false

            function InputManager.captureNextKey(
                onCaptured: CaptureCallback__DARKLUA_TYPE_z,
                onCancelled: (() -> ())?
            )
                pendingCapture = {
                    onCaptured = onCaptured,
                    onCancelled = onCancelled,
                }
            end
            function InputManager.cancelCapture()
                local capture = pendingCapture

                pendingCapture = nil

                if capture and capture.onCancelled then
                    capture.onCancelled()
                end
            end
            function InputManager.isCapturing(): boolean
                return pendingCapture ~= nil
            end
            function InputManager.onKeyDown(callback: (keyCode:Enum.KeyCode) -> ())
                table.insert(keyDownListeners, callback)
            end
            function InputManager.onKeyUp(callback: (keyCode:Enum.KeyCode) -> ())
                table.insert(keyUpListeners, callback)
            end
            function InputManager.onMenuToggle(callback: () -> ())
                table.insert(menuToggleListeners, callback)
            end
            function InputManager.start()
                if started then
                    return
                end

                started = true

                Connections.bind(UserInputService.InputBegan, function(
                    input: InputObject,
                    gameProcessed: boolean
                )
                    if input.UserInputType ~= Enum.UserInputType.Keyboard then
                        return
                    end
                    if pendingCapture then
                        if input.KeyCode ~= Enum.KeyCode.Unknown then
                            local capture = pendingCapture::PendingCapture__DARKLUA_TYPE_A

                            pendingCapture = nil

                            if input.KeyCode == Enum.KeyCode.Escape then
                                if capture.onCancelled then
                                    capture.onCancelled()
                                end
                            else
                                capture.onCaptured(input.KeyCode)
                            end
                        end

                        return
                    end
                    if gameProcessed then
                        return
                    end
                    if input.KeyCode == MENU_KEY then
                        for _, callback in menuToggleListeners do
                            task.spawn(callback)
                        end

                        return
                    end

                    for _, callback in keyDownListeners do
                        task.spawn(callback, input.KeyCode)
                    end
                end)
                Connections.bind(UserInputService.InputEnded, function(
                    input: InputObject
                )
                    if input.UserInputType ~= Enum.UserInputType.Keyboard then
                        return
                    end

                    for _, callback in keyUpListeners do
                        task.spawn(callback, input.KeyCode)
                    end
                end)
            end

            return InputManager
        end

        function __DARKLUA_BUNDLE_MODULES.u(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.u

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.u = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local Theme = __DARKLUA_BUNDLE_MODULES.d()
            local UI = __DARKLUA_BUNDLE_MODULES.e()
            local Layout = __DARKLUA_BUNDLE_MODULES.p()
            local Fade = __DARKLUA_BUNDLE_MODULES.n()
            local Shadow = __DARKLUA_BUNDLE_MODULES.i()
            local InputManager = __DARKLUA_BUNDLE_MODULES.u()
            local WIDTH = 150
            local HEIGHT = 76
            local KeybindPicker = {}

            function KeybindPicker.create(
                mainWindow: Frame,
                onNewBind: (path:string, name:string, key:Enum.KeyCode, mode:string) -> ()
            ): KeybindPickerApi__DARKLUA_TYPE_B
                local window = UI.new('Frame', {
                    Name = 'keybind_picker_window',
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 908, 0, 324),
                    Size = UDim2.new(0, WIDTH, 0, HEIGHT),
                    ZIndex = 55,
                    Visible = false,
                    Parent = mainWindow,
                })::Frame

                Theme.bind(window, 'BackgroundColor3', 'Popup')
                UI.corner(window, 10)
                UI.stroke(window, Color3.fromRGB(48, 48, 58), 0.45)

                local shadow = Shadow.create(window)
                local newBindField = UI.new('Frame', {
                    Name = 'NewBindField',
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 8, 0, 8),
                    Size = UDim2.new(0, WIDTH - 16, 0, 26),
                    ZIndex = 56,
                    Parent = window,
                })::Frame

                Theme.bind(newBindField, 'BackgroundColor3', 'Field')
                UI.corner(newBindField, 7)
                UI.new('ImageLabel', {
                    Name = 'IMG_BindIcon',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 9, 0, 7),
                    Size = UDim2.new(0, 12, 0, 12),
                    ZIndex = 56,
                    Image = 'rbxassetid://122878673716704',
                    ImageColor3 = Color3.fromRGB(138, 138, 148),
                    ScaleType = Enum.ScaleType.Fit,
                    Parent = newBindField,
                })

                local placeholder = UI.new('TextLabel', {
                    Name = 'Placeholder',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 27, 0, 0),
                    Size = UDim2.new(0, 82, 0, 26),
                    ZIndex = 56,
                    FontFace = Theme.Font.Medium,
                    Text = 'New bind',
                    TextColor3 = Color3.fromRGB(138, 138, 148),
                    TextSize = 12,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    Parent = newBindField,
                })::TextLabel

                UI.new('ImageLabel', {
                    Name = 'IMG_Plus',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, WIDTH - 34, 0, 7),
                    Size = UDim2.new(0, 12, 0, 12),
                    ZIndex = 56,
                    Image = 'rbxassetid://101123124881873',
                    ImageColor3 = Color3.fromRGB(138, 138, 148),
                    ScaleType = Enum.ScaleType.Fit,
                    Parent = newBindField,
                })

                local buttonWidth = (WIDTH - 16 - 4) // 2
                local toggleButton = UI.new('Frame', {
                    Name = 'ToggleButton',
                    BackgroundColor3 = Color3.fromRGB(123, 104, 246),
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 8, 0, 42),
                    Size = UDim2.new(0, buttonWidth, 0, 26),
                    ZIndex = 56,
                    Parent = window,
                })::Frame

                UI.corner(toggleButton, 7)

                local toggleLabel = UI.new('TextLabel', {
                    Name = 'Label',
                    BackgroundTransparency = 1,
                    Size = UDim2.new(1, 0, 1, 0),
                    ZIndex = 56,
                    FontFace = Theme.Font.SemiBold,
                    Text = 'Toggle',
                    TextColor3 = Color3.fromRGB(34, 29, 72),
                    TextSize = 12,
                    Parent = toggleButton,
                })::TextLabel
                local holdButton = UI.new('Frame', {
                    Name = 'HoldButton',
                    BackgroundColor3 = Theme.Color.Field,
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 8 + buttonWidth + 4, 0, 42),
                    Size = UDim2.new(0, buttonWidth, 0, 26),
                    ZIndex = 56,
                    Parent = window,
                })::Frame

                UI.corner(holdButton, 7)

                local holdLabel = UI.new('TextLabel', {
                    Name = 'Label',
                    BackgroundTransparency = 1,
                    Size = UDim2.new(1, 0, 1, 0),
                    ZIndex = 56,
                    FontFace = Theme.Font.SemiBold,
                    Text = 'Hold',
                    TextColor3 = Color3.fromRGB(244, 244, 248),
                    TextSize = 12,
                    Parent = holdButton,
                })::TextLabel
                local mode = 'Toggle'
                local pickerTarget: {path: string, name: string}? = nil

                local function setMode(nextMode: string)
                    mode = nextMode

                    local toggleActive = mode == 'Toggle'

                    toggleButton.BackgroundColor3 = toggleActive and Color3.fromRGB(123, 104, 246) or Theme.Color.Field
                    toggleLabel.TextColor3 = toggleActive and Color3.fromRGB(34, 29, 72) or Color3.fromRGB(244, 244, 248)
                    holdButton.BackgroundColor3 = toggleActive and Theme.Color.Field or Color3.fromRGB(123, 104, 246)
                    holdLabel.TextColor3 = toggleActive and Color3.fromRGB(244, 244, 248) or Color3.fromRGB(34, 29, 72)
                end
                local function close()
                    placeholder.Text = 'New bind'

                    shadow:Render(false)
                    Fade.popupOut(window)
                end
                local function open(
                    anchorRow: Frame,
                    path: string,
                    name: string
                )
                    pickerTarget = {
                        path = path,
                        name = name,
                    }

                    setMode('Toggle')

                    local rx, ry = Layout.relativeTo(anchorRow, mainWindow)
                    local position = UDim2.new(0, math.min(rx + 100, 560), 0, ry + 34)

                    Fade.popupIn(window, position)
                    shadow:Render(true)
                end

                UI.hitButton(toggleButton, nil, nil, 57).MouseButton1Click:Connect(function(
                )
                    setMode('Toggle')
                end)
                UI.hitButton(holdButton, nil, nil, 57).MouseButton1Click:Connect(function(
                )
                    setMode('Hold')
                end)
                UI.hitButton(newBindField, nil, nil, 57).MouseButton1Click:Connect(function(
                )
                    if not pickerTarget then
                        return
                    end

                    placeholder.Text = 'Press a key...'

                    local target = pickerTarget

                    InputManager.captureNextKey(function(keyCode)
                        onNewBind(target.path, target.name, keyCode, mode)
                        close()
                    end, function()
                        placeholder.Text = 'New bind'
                    end)
                end)

                return {
                    open = open,
                    close = close,
                }
            end

            return KeybindPicker
        end

        function __DARKLUA_BUNDLE_MODULES.v(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.v

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.v = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local Theme = __DARKLUA_BUNDLE_MODULES.d()
            local UI = __DARKLUA_BUNDLE_MODULES.e()
            local Layout = __DARKLUA_BUNDLE_MODULES.p()
            local Fade = __DARKLUA_BUNDLE_MODULES.n()
            local Animate = __DARKLUA_BUNDLE_MODULES.j()
            local Shadow = __DARKLUA_BUNDLE_MODULES.i()
            local Types = __DARKLUA_BUNDLE_MODULES.b()
            local ModuleSelector = {}

            function ModuleSelector.create(
                mainWindow: Frame,
                tabModules: {[string]: {ModuleDefinition__DARKLUA_TYPE_k}},
                getSelectedModule: (tab:string) -> string?,
                onSelect: (tab:string, moduleName:string) -> ()
            ): ModuleSelectorApi__DARKLUA_TYPE_C
                local window = UI.new('Frame', {
                    Name = 'module_selector_window',
                    AnchorPoint = Vector2.new(0, 0.5),
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 0, 0, 0),
                    Size = UDim2.new(0, 170, 0, 272),
                    ZIndex = 70,
                    Visible = false,
                    Parent = mainWindow,
                })::Frame

                Theme.bind(window, 'BackgroundColor3', 'Popup')
                UI.corner(window, 12)
                UI.stroke(window, Color3.fromRGB(46, 46, 56), 0.45)

                local shadow = Shadow.create(window)
                local openTab: string? = nil
                local lastItems: {GuiObject} = {}

                local function close()
                    openTab = nil

                    shadow:Render(false)
                    Fade.popupOut(window)
                end
                local function rebuild(tab: string)
                    lastItems = {}

                    for _, child in window:GetChildren()do
                        if child:IsA('Frame') then
                            child:Destroy()
                        end
                    end

                    local modules = tabModules[tab] or {}
                    local current = getSelectedModule(tab) or (modules[1] and modules[1].name)

                    for index, moduleDef in modules do
                        local selected = moduleDef.name == current
                        local item = UI.new('Frame', {
                            Name = 'Item_' .. moduleDef.name,
                            BackgroundTransparency = 1,
                            Position = UDim2.new(0, 0, 0, 10 + (index - 1) * 28),
                            Size = UDim2.new(0, 170, 0, 28),
                            ZIndex = 70,
                            Parent = window,
                        })::Frame
                        local highlight = UI.new('Frame', {
                            Name = 'Highlight',
                            BackgroundColor3 = Theme.Color.Highlight,
                            BackgroundTransparency = selected and 0 or 1,
                            BorderSizePixel = 0,
                            Position = UDim2.new(0, 6, 0, 2),
                            Size = UDim2.new(0, 158, 0, 24),
                            ZIndex = 70,
                            Parent = item,
                        })::Frame

                        UI.corner(highlight, 8)

                        local label = UI.new('TextLabel', {
                            Name = 'Label',
                            BackgroundTransparency = 1,
                            Position = UDim2.new(0, 16, 0, 0),
                            Size = UDim2.new(0, 120, 0, 28),
                            ZIndex = 71,
                            FontFace = selected and Theme.Font.SemiBold or Theme.Font.Medium,
                            Text = moduleDef.name,
                            TextColor3 = selected and Theme.Color.Text or Theme.Color.Muted,
                            TextSize = 12,
                            TextXAlignment = Enum.TextXAlignment.Left,
                            Parent = item,
                        })::TextLabel

                        if moduleDef.caret then
                            UI.new('ImageLabel', {
                                Name = 'IMG_Caret',
                                BackgroundTransparency = 1,
                                Position = UDim2.new(0, 146, 0, 9),
                                Size = UDim2.new(0, 10, 0, 10),
                                ZIndex = 71,
                                Image = 'rbxassetid://10709790948',
                                ImageColor3 = Theme.Color.Muted,
                                ScaleType = Enum.ScaleType.Fit,
                                Parent = item,
                            })
                        end
                        if selected then
                            UI.new('ImageLabel', {
                                Name = 'indicator',
                                BackgroundTransparency = 1,
                                Position = UDim2.new(0, 0, 0.5, -6),
                                Size = UDim2.new(0, 3, 0, 13),
                                ZIndex = 71,
                                Image = 'rbxassetid://90362801488029',
                                Parent = item,
                            })
                        end

                        local hit = UI.hitButton(item, nil, nil, 72)

                        hit.MouseEnter:Connect(function()
                            if moduleDef.name ~= getSelectedModule(tab) then
                                Animate.tween(highlight, {BackgroundTransparency = 0.55}, Animate.Quick)
                                Animate.tween(label, {
                                    TextColor3 = Color3.fromRGB(210, 210, 218),
                                }, Animate.Quick)

                                label.FontFace = Theme.Font.SemiBold
                            end
                        end)
                        hit.MouseLeave:Connect(function()
                            if moduleDef.name ~= getSelectedModule(tab) then
                                Animate.tween(highlight, {BackgroundTransparency = 1}, Animate.Quick)
                                Animate.tween(label, {
                                    TextColor3 = Theme.Color.Muted,
                                }, Animate.Quick)

                                label.FontFace = Theme.Font.Medium
                            end
                        end)
                        hit.MouseButton1Click:Connect(function()
                            onSelect(tab, moduleDef.name)
                            close()
                        end)
                        table.insert(lastItems, item)
                    end

                    window.Size = UDim2.new(0, 170, 0, 20 + #modules * 28)
                end
                local function open(tab: string, navFrame: Frame)
                    if window.Visible and openTab == tab then
                        close()

                        return
                    end

                    openTab = tab

                    rebuild(tab)

                    local rx, ry = Layout.relativeTo(navFrame, mainWindow)
                    local position = UDim2.fromOffset(rx + navFrame.AbsoluteSize.X + 24, ry + navFrame.AbsoluteSize.Y / 2)

                    Fade.popupIn(window, position)
                    Fade.stagger(lastItems, 0.02)
                    shadow:Render(true)
                end

                return {
                    open = open,
                    close = close,
                }
            end

            return ModuleSelector
        end

        function __DARKLUA_BUNDLE_MODULES.w(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.w

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.w = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local DropdownPopup = __DARKLUA_BUNDLE_MODULES.q()
            local ColorPicker = __DARKLUA_BUNDLE_MODULES.t()
            local KeybindPicker = __DARKLUA_BUNDLE_MODULES.v()
            local ModuleSelector = __DARKLUA_BUNDLE_MODULES.w()
            local Types = __DARKLUA_BUNDLE_MODULES.b()
            local Popups = {}

            function Popups.create(
                mainWindow: Frame,
                tabModules: {[string]: {ModuleDefinition__DARKLUA_TYPE_k}},
                getSelectedModule: (tab:string) -> string?,
                onModuleSelected: (tab:string, moduleName:string) -> (),
                onNewBind: (path:string, name:string, key:Enum.KeyCode, mode:string) -> ()
            ): PopupsApi__DARKLUA_TYPE_D
                local dropdown = DropdownPopup.create(mainWindow)
                local colorPicker = ColorPicker.create(mainWindow)
                local keybindPicker = KeybindPicker.create(mainWindow, onNewBind)
                local moduleSelector = ModuleSelector.create(mainWindow, tabModules, getSelectedModule, onModuleSelected)

                local function closeAll()
                    dropdown.close()
                    colorPicker.close()
                    keybindPicker.close()
                    moduleSelector.close()
                end

                return {
                    closeAll = closeAll,
                    Dropdown = {
                        open = function(anchor, options, current, onPick)
                            colorPicker.close()
                            keybindPicker.close()
                            dropdown.open(anchor, options, current, onPick)
                        end,
                        close = dropdown.close,
                    },
                    ColorPicker = {
                        open = function(swatch, path, title)
                            dropdown.close()
                            keybindPicker.close()
                            colorPicker.open(swatch, path, title)
                        end,
                        close = colorPicker.close,
                    },
                    KeybindPicker = {
                        open = function(anchorRow, path, name)
                            dropdown.close()
                            colorPicker.close()
                            keybindPicker.open(anchorRow, path, name)
                        end,
                        close = keybindPicker.close,
                    },
                    ModuleSelector = {
                        open = function(tab, navFrame)
                            dropdown.close()
                            colorPicker.close()
                            keybindPicker.close()
                            moduleSelector.open(tab, navFrame)
                        end,
                        close = moduleSelector.close,
                    },
                }
            end

            return Popups
        end

        function __DARKLUA_BUNDLE_MODULES.x(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.x

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.x = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local Theme = __DARKLUA_BUNDLE_MODULES.d()
            local UI = __DARKLUA_BUNDLE_MODULES.e()
            local Fade = __DARKLUA_BUNDLE_MODULES.n()
            local Animate = __DARKLUA_BUNDLE_MODULES.j()
            local State = __DARKLUA_BUNDLE_MODULES.c()
            local Types = __DARKLUA_BUNDLE_MODULES.b()
            local DEFAULT_PRESETS = {
                'Legit PvP',
                'Rage HVH',
                'Trolling Config',
                'Stealth RP',
                'Vehicle God',
            }
            local Presets = {}

            function Presets.create(
                mainWindow: Frame,
                presetButton: Frame,
                presetButtonLabel: TextLabel
            ): PresetsApi__DARKLUA_TYPE_E
                local window = UI.new('Frame', {
                    Name = 'name_config_window',
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 737, 0, 3),
                    Size = UDim2.new(0, 236, 0, 300),
                    ZIndex = 60,
                    Visible = false,
                    Parent = mainWindow,
                })::Frame

                Theme.bind(window, 'BackgroundColor3', 'Background')
                UI.corner(window, 12)
                UI.stroke(window, Color3.fromRGB(46, 46, 57), 0.4)

                local BASE_POSITION = UDim2.new(0, 737, 0, 3)

                local function setOpen(open: boolean)
                    if open then
                        Fade.popupIn(window, BASE_POSITION)
                    else
                        Fade.popupOut(window)
                    end
                end

                UI.new('ImageLabel', {
                    Name = 'IMG_CloudIcon',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 14, 0, 14),
                    Size = UDim2.new(0, 18, 0, 18),
                    ZIndex = 61,
                    Image = 'rbxassetid://70621328130295',
                    ImageColor3 = Color3.fromRGB(150, 150, 161),
                    ScaleType = Enum.ScaleType.Fit,
                    Parent = window,
                })
                UI.new('TextLabel', {
                    Name = 'Title',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 40, 0, 10),
                    Size = UDim2.new(0, 120, 0, 24),
                    ZIndex = 61,
                    FontFace = Theme.Font.SemiBold,
                    Text = 'Presets',
                    TextColor3 = Theme.Color.Text,
                    TextSize = 14,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    Parent = window,
                })

                local addButton = UI.new('Frame', {
                    Name = 'AddButton',
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 196, 0, 11),
                    Size = UDim2.new(0, 26, 0, 26),
                    ZIndex = 61,
                    Parent = window,
                })::Frame

                Theme.bind(addButton, 'BackgroundColor3', 'PresetField')
                UI.corner(addButton, 7)
                UI.new('ImageLabel', {
                    Name = 'IMG_Add',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 6, 0, 6),
                    Size = UDim2.new(0, 14, 0, 14),
                    ZIndex = 61,
                    Image = 'rbxassetid://101123124881873',
                    ImageColor3 = Theme.Color.Text,
                    ScaleType = Enum.ScaleType.Fit,
                    Parent = addButton,
                })

                local searchField = UI.new('Frame', {
                    Name = 'SearchField',
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 14, 0, 50),
                    Size = UDim2.new(0, 174, 0, 34),
                    ZIndex = 61,
                    Parent = window,
                })::Frame

                Theme.bind(searchField, 'BackgroundColor3', 'PresetField')
                UI.corner(searchField, 8)
                UI.new('ImageLabel', {
                    Name = 'IMG_SearchIcon',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 10, 0, 10),
                    Size = UDim2.new(0, 14, 0, 14),
                    ZIndex = 61,
                    Image = 'rbxassetid://72296609649861',
                    ImageColor3 = Color3.fromRGB(150, 150, 161),
                    ScaleType = Enum.ScaleType.Fit,
                    Parent = searchField,
                })

                local searchBox = UI.new('TextBox', {
                    Name = 'Input',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 32, 0, 0),
                    Size = UDim2.new(0, 130, 0, 34),
                    ZIndex = 61,
                    FontFace = Theme.Font.Medium,
                    Text = '',
                    PlaceholderText = 'Search',
                    PlaceholderColor3 = Color3.fromRGB(120, 120, 131),
                    TextColor3 = Theme.Color.Text,
                    TextSize = 12,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    ClearTextOnFocus = false,
                    Parent = searchField,
                })::TextBox
                local sortButton = UI.new('Frame', {
                    Name = 'SortButton',
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 192, 0, 50),
                    Size = UDim2.new(0, 30, 0, 34),
                    ZIndex = 61,
                    Parent = window,
                })::Frame

                Theme.bind(sortButton, 'BackgroundColor3', 'PresetField')
                UI.corner(sortButton, 8)

                local sortIcon = UI.new('ImageLabel', {
                    Name = 'IMG_SortIcon',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 8, 0, 10),
                    Size = UDim2.new(0, 14, 0, 14),
                    ZIndex = 61,
                    Image = 'rbxassetid://117405374619280',
                    ImageColor3 = Color3.fromRGB(150, 150, 161),
                    ScaleType = Enum.ScaleType.Fit,
                    Parent = sortButton,
                })::ImageLabel
                local indicator = UI.new('ImageLabel', {
                    Name = 'indicator',
                    BackgroundColor3 = Color3.fromRGB(255, 255, 255),
                    BackgroundTransparency = 1,
                    BorderSizePixel = 0,
                    Position = UDim2.new(-0.0462963, 0, 0.242412, 0),
                    Size = UDim2.new(0, 3, 0, 18),
                    ZIndex = 61,
                    Image = 'rbxassetid://90362801488029',
                })::ImageLabel
                local presets: {PresetEntry__DARKLUA_TYPE_F} = {}
                local activePreset: PresetEntry__DARKLUA_TYPE_F? = nil
                local sortAscending = false
                local applying = false

                local function renderRows()
                    local query = searchBox.Text:lower()
                    local visible: {PresetEntry__DARKLUA_TYPE_F} = {}

                    for _, preset in presets do
                        if query == '' or preset.name:lower():find(query, 1, true) then
                            table.insert(visible, preset)
                        end
                    end

                    if sortAscending then
                        table.sort(visible, function(a, b)
                            return a.name:lower() < b.name:lower()
                        end)
                    end

                    for _, preset in presets do
                        preset.row.Visible = false
                    end
                    for index, preset in visible do
                        preset.row.Visible = true
                        preset.row.Position = UDim2.new(0, 10, 0, 98 + (index - 1) * 40)
                    end

                    window.Size = UDim2.new(0, 236, 0, math.max(300, 98 + #visible * 40 + 10))
                end
                local function renderStates()
                    for _, preset in presets do
                        local selected = preset == activePreset

                        preset.row.BackgroundTransparency = selected and 0 or 1

                        preset.row:SetAttribute('fadeOriginal_BackgroundTransparency', selected and 0 or 1)

                        preset.nameLabel.FontFace = selected and Theme.Font.SemiBold or Theme.Font.Medium
                        preset.nameLabel.TextColor3 = selected and Theme.Color.Text or Color3.fromRGB(150, 150, 161)
                        preset.editIcon.ImageColor3 = selected and Color3.fromRGB(150, 150, 161) or Color3.fromRGB(120, 120, 131)
                        preset.deleteIcon.ImageColor3 = selected and Color3.fromRGB(224, 82, 76) or Color3.fromRGB(120, 120, 131)
                    end

                    if activePreset then
                        indicator.Parent = activePreset.row
                        presetButtonLabel.Text = activePreset.name
                    end
                end
                local function selectPreset(preset: PresetEntry__DARKLUA_TYPE_F)
                    if activePreset == preset then
                        return
                    end
                    if activePreset then
                        activePreset.snapshot = State.snapshot()
                    end

                    activePreset = preset
                    applying = true

                    State.applySnapshot(preset.snapshot)

                    applying = false

                    renderStates()
                end

                local addPreset: (name:string, snapshot:Config__DARKLUA_TYPE_g?, makeActive:boolean?) -> PresetEntry__DARKLUA_TYPE_F

                addPreset = function(name, snapshot, makeActive)
                    local row = UI.new('Frame', {
                        Name = 'Config_' .. name:gsub('%s', '_'),
                        BackgroundTransparency = 1,
                        BorderSizePixel = 0,
                        Position = UDim2.new(0, 10, 0, 98),
                        Size = UDim2.new(0, 216, 0, 34),
                        ZIndex = 61,
                        Parent = window,
                    })::Frame

                    Theme.bind(row, 'BackgroundColor3', 'PresetRow')
                    UI.corner(row, 8)

                    local nameLabel = UI.new('TextLabel', {
                        Name = 'Name',
                        BackgroundTransparency = 1,
                        Position = UDim2.new(0, 14, 0, 0),
                        Size = UDim2.new(0, 130, 0, 34),
                        ZIndex = 61,
                        FontFace = Theme.Font.Medium,
                        Text = name,
                        TextColor3 = Color3.fromRGB(150, 150, 161),
                        TextSize = 12,
                        TextXAlignment = Enum.TextXAlignment.Left,
                        TextTruncate = Enum.TextTruncate.AtEnd,
                        Parent = row,
                    })::TextLabel
                    local editIcon = UI.new('ImageLabel', {
                        Name = 'IMG_Edit',
                        BackgroundTransparency = 1,
                        Position = UDim2.new(0, 158, 0, 9),
                        Size = UDim2.new(0, 16, 0, 16),
                        ZIndex = 61,
                        Image = 'rbxassetid://131819263664272',
                        ImageColor3 = Color3.fromRGB(120, 120, 131),
                        ScaleType = Enum.ScaleType.Fit,
                        Parent = row,
                    })::ImageLabel
                    local deleteIcon = UI.new('ImageLabel', {
                        Name = 'IMG_Delete',
                        BackgroundTransparency = 1,
                        Position = UDim2.new(0, 186, 0, 9),
                        Size = UDim2.new(0, 16, 0, 16),
                        ZIndex = 61,
                        Image = 'rbxassetid://126010725826757',
                        ImageColor3 = Color3.fromRGB(120, 120, 131),
                        ScaleType = Enum.ScaleType.Fit,
                        Parent = row,
                    })::ImageLabel
                    local preset: PresetEntry__DARKLUA_TYPE_F = {
                        name = name,
                        snapshot = snapshot or State.snapshot(),
                        row = row,
                        nameLabel = nameLabel,
                        editIcon = editIcon,
                        deleteIcon = deleteIcon,
                    }

                    table.insert(presets, preset)

                    local selectHit = UI.hitButton(row, UDim2.new(0, 144, 1, 0), nil, 62)

                    selectHit.MouseEnter:Connect(function()
                        if preset ~= activePreset then
                            Animate.tween(row, {BackgroundTransparency = 0.6}, Animate.Quick)
                        end
                    end)
                    selectHit.MouseLeave:Connect(function()
                        if preset ~= activePreset then
                            Animate.tween(row, {BackgroundTransparency = 1}, Animate.Quick)
                        end
                    end)
                    selectHit.MouseButton1Click:Connect(function()
                        selectPreset(preset)
                    end)
                    UI.hitButton(row, UDim2.new(0, 24, 0, 24), UDim2.new(0, 154, 0, 5), 63).MouseButton1Click:Connect(function(
                    )
                        local box = UI.new('TextBox', {
                            BackgroundColor3 = Theme.Color.Field,
                            BorderSizePixel = 0,
                            Position = UDim2.new(0, 10, 0, 5),
                            Size = UDim2.new(0, 138, 0, 24),
                            ZIndex = 64,
                            FontFace = Theme.Font.Medium,
                            Text = preset.name,
                            TextColor3 = Theme.Color.Text,
                            TextSize = 12,
                            TextXAlignment = Enum.TextXAlignment.Left,
                            ClearTextOnFocus = false,
                            Parent = row,
                        })::TextBox

                        UI.corner(box, 6)
                        box:CaptureFocus()
                        box.FocusLost:Connect(function()
                            local trimmed = box.Text:gsub('^%s+', ''):gsub('%s+$', '')

                            if trimmed ~= '' then
                                preset.name = trimmed
                                nameLabel.Text = trimmed

                                if preset == activePreset then
                                    presetButtonLabel.Text = trimmed
                                end
                            end

                            box:Destroy()
                            renderRows()
                        end)
                    end)
                    UI.hitButton(row, UDim2.new(0, 24, 0, 24), UDim2.new(0, 182, 0, 5), 63).MouseButton1Click:Connect(function(
                    )
                        if #presets <= 1 then
                            return
                        end

                        for index, entry in presets do
                            if entry == preset then
                                table.remove(presets, index)

                                break
                            end
                        end

                        row:Destroy()

                        if activePreset == preset then
                            activePreset = nil

                            selectPreset(presets[1])
                        end

                        renderRows()
                    end)

                    if makeActive then
                        activePreset = preset
                    end

                    renderRows()
                    renderStates()

                    return preset
                end

                State.onChanged(function()
                    if applying then
                        return
                    end
                    if activePreset then
                        activePreset.snapshot = State.snapshot()
                    end
                end)

                for index, name in DEFAULT_PRESETS do
                    addPreset(name, nil, index == 1)
                end

                local presetHit = UI.hitButton(presetButton, nil, nil, 3)

                presetHit.MouseEnter:Connect(function()
                    Animate.tween(presetButton, {BackgroundTransparency = 0}, Animate.Quick)
                end)
                presetHit.MouseLeave:Connect(function()
                    Animate.tween(presetButton, {BackgroundTransparency = 0.1}, Animate.Quick)
                end)
                presetHit.MouseButton1Click:Connect(function()
                    setOpen(not window.Visible)
                end)
                UI.hitButton(addButton, nil, nil, 62).MouseButton1Click:Connect(function(
                )
                    local preset = addPreset('Config ' .. (#presets + 1), State.snapshot())

                    selectPreset(preset)
                end)
                UI.hitButton(sortButton, nil, nil, 62).MouseButton1Click:Connect(function(
                )
                    sortAscending = not sortAscending
                    sortIcon.ImageColor3 = sortAscending and Theme.Color.Accent or Color3.fromRGB(150, 150, 161)

                    renderRows()
                end)
                searchBox:GetPropertyChangedSignal('Text'):Connect(renderRows)

                return {
                    toggle = function()
                        setOpen(not window.Visible)
                    end,
                }
            end

            return Presets
        end

        function __DARKLUA_BUNDLE_MODULES.y(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.y

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.y = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local Theme = __DARKLUA_BUNDLE_MODULES.d()
            local UI = __DARKLUA_BUNDLE_MODULES.e()
            local Drag = __DARKLUA_BUNDLE_MODULES.r()
            local State = __DARKLUA_BUNDLE_MODULES.c()
            local InputManager = __DARKLUA_BUNDLE_MODULES.u()
            local Types = __DARKLUA_BUNDLE_MODULES.b()
            local ROW_HEIGHT = 30
            local HEADER_HEIGHT = 40
            local KeybindList = {}

            function KeybindList.create(rootGui: Instance): KeybindListApi__DARKLUA_TYPE_G
                local window = UI.new('Frame', {
                    Name = 'keybind_list_window',
                    AnchorPoint = Vector2.new(1, 0),
                    BorderSizePixel = 0,
                    Position = UDim2.new(0.990808, -24, -0.635019, 543),
                    Size = UDim2.new(0, 348, 0, 108),
                    Parent = rootGui,
                })::Frame

                Theme.bind(window, 'BackgroundColor3', 'Background')
                UI.corner(window, 12)
                UI.stroke(window, Color3.fromRGB(46, 46, 56), 0.4)
                UI.new('ImageLabel', {
                    Name = 'IMG_KeybindsIcon',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 14, 0, 11),
                    Size = UDim2.new(0, 18, 0, 18),
                    Image = 'rbxassetid://129431925610335',
                    ImageColor3 = Color3.fromRGB(148, 148, 158),
                    ScaleType = Enum.ScaleType.Fit,
                    Parent = window,
                })
                UI.new('TextLabel', {
                    Name = 'Title',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 44, 0, 0),
                    Size = UDim2.new(0, 120, 0, 40),
                    FontFace = Theme.Font.SemiBold,
                    Text = 'Keybinds',
                    TextColor3 = Color3.fromRGB(240, 240, 245),
                    TextSize = 13,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    Parent = window,
                })

                local pinIcon = UI.new('ImageLabel', {
                    Name = 'IMG_Pin',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 318, 0, 12),
                    Size = UDim2.new(0, 16, 0, 16),
                    Image = 'rbxassetid://84152083728286',
                    ImageColor3 = Color3.fromRGB(108, 108, 118),
                    ScaleType = Enum.ScaleType.Fit,
                    Parent = window,
                })::ImageLabel

                Drag.makeDraggable(window)

                local binds: {Bind__DARKLUA_TYPE_i} = {}
                local pinned = false

                local function refreshSize()
                    window.Size = UDim2.new(0, 348, 0, HEADER_HEIGHT + #binds * ROW_HEIGHT + 8)
                end
                local function addBindRow(bind: Bind__DARKLUA_TYPE_i)
                    local index = #binds + 1
                    local row = UI.new('Frame', {
                        Name = 'Bind_' .. bind.Name:gsub('%s', '_'),
                        BackgroundTransparency = 1,
                        Position = UDim2.new(0, 0, 0, HEADER_HEIGHT + (index - 1) * ROW_HEIGHT),
                        Size = UDim2.new(0, 348, 0, 28),
                        Parent = window,
                    })::Frame

                    UI.new('TextLabel', {
                        Name = 'Label',
                        BackgroundTransparency = 1,
                        Position = UDim2.new(0, 16, 0, 0),
                        Size = UDim2.new(0, 160, 0, 28),
                        FontFace = Theme.Font.Medium,
                        Text = bind.Name,
                        TextColor3 = Color3.fromRGB(148, 148, 158),
                        TextSize = 12,
                        TextXAlignment = Enum.TextXAlignment.Left,
                        Parent = row,
                    })

                    local modeLabel = UI.new('TextLabel', {
                        Name = 'Mode',
                        BackgroundTransparency = 1,
                        Position = UDim2.new(0, 232, 0, 0),
                        Size = UDim2.new(0, 60, 0, 28),
                        FontFace = Theme.Font.Medium,
                        Text = bind.Mode,
                        TextColor3 = Color3.fromRGB(148, 148, 158),
                        TextSize = 12,
                        TextXAlignment = Enum.TextXAlignment.Right,
                        Parent = row,
                    })::TextLabel
                    local keyLabel = UI.new('TextLabel', {
                        Name = 'Key',
                        BackgroundTransparency = 1,
                        Position = UDim2.new(0, 300, 0, 0),
                        Size = UDim2.new(0, 14, 0, 28),
                        FontFace = Theme.Font.SemiBold,
                        Text = bind.Key.Name,
                        TextColor3 = Color3.fromRGB(240, 240, 245),
                        TextSize = 12,
                        Parent = row,
                    })::TextLabel

                    UI.new('TextLabel', {
                        Name = 'Dots',
                        BackgroundTransparency = 1,
                        Position = UDim2.new(0, 322, 0, 0),
                        Size = UDim2.new(0, 12, 0, 28),
                        FontFace = Theme.Font.Bold,
                        Text = '\u{b7}\u{b7}\u{b7}',
                        TextColor3 = Color3.fromRGB(108, 108, 118),
                        TextSize = 13,
                        Parent = row,
                    })
                    UI.hitButton(row, UDim2.new(0, 40, 0, 28), UDim2.new(0, 308, 0, 0), 3).MouseButton1Click:Connect(function(
                    )
                        keyLabel.Text = '...'

                        InputManager.captureNextKey(function(keyCode)
                            bind.Key = keyCode
                            keyLabel.Text = keyCode.Name
                        end, function()
                            keyLabel.Text = bind.Key.Name
                        end)
                    end)

                    bind.row, bind.keyLabel, bind.modeLabel = row, keyLabel, modeLabel

                    table.insert(binds, bind)
                    refreshSize()
                end
                local function addBind(
                    path: string,
                    name: string,
                    key: Enum.KeyCode,
                    mode: BindMode__DARKLUA_TYPE_h
                )
                    addBindRow({
                        Key = key,
                        Mode = mode,
                        Path = path,
                        Name = name,
                    })
                end

                InputManager.onKeyDown(function(keyCode)
                    for _, bind in binds do
                        if bind.Key == keyCode then
                            local control = State.Controls[bind.Path]

                            if control then
                                if bind.Mode == 'Toggle' then
                                    control.Set(not control.Get())
                                else
                                    control.Set(true)
                                end
                            end
                        end
                    end
                end)
                InputManager.onKeyUp(function(keyCode)
                    for _, bind in binds do
                        if bind.Key == keyCode and bind.Mode == 'Hold' then
                            local control = State.Controls[bind.Path]

                            if control then
                                control.Set(false)
                            end
                        end
                    end
                end)
                UI.hitButton(pinIcon, UDim2.new(0, 24, 0, 24), UDim2.new(0, -4, 0, 
-4), 3).MouseButton1Click:Connect(function()
                    pinned = not pinned
                    pinIcon.ImageColor3 = pinned and Theme.Color.Accent or Color3.fromRGB(108, 108, 118)
                end)
                addBind('Movement.Bhop', 'Enable bhop', Enum.KeyCode.E, 'Toggle')
                addBind('Movement.Fly', 'Enable fly', Enum.KeyCode.W, 'Toggle')

                for _, path in {
                    'Movement.Bhop',
                    'Movement.Fly',
                }do
                    if not State.Controls[path] then
                        State.Controls[path] = {
                            Set = function(value)
                                State.set(path, value)
                            end,
                            Get = function()
                                return State.get(path)
                            end,
                        }
                    end
                end

                return {addBind = addBind}
            end

            return KeybindList
        end

        function __DARKLUA_BUNDLE_MODULES.z(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.z

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.z = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local Theme = __DARKLUA_BUNDLE_MODULES.d()
            local UI = __DARKLUA_BUNDLE_MODULES.e()
            local Shadow = __DARKLUA_BUNDLE_MODULES.i()
            local Animate = __DARKLUA_BUNDLE_MODULES.j()
            local Fade = __DARKLUA_BUNDLE_MODULES.n()
            local WIDTH = 260
            local EDGE_MARGIN = 24
            local GAP = 8
            local ENTER_OFFSET = 36
            local MAX_STACK = 5
            local Notifications = {}

            function Notifications.create(rootGui: Instance): NotificationsApi__DARKLUA_TYPE_H
                local toasts: {Toast__DARKLUA_TYPE_I} = {}

                local function slotPosition(index: number): UDim2
                    local offset = 0

                    for i = #toasts, index + 1, -1 do
                        offset += toasts[i].height + GAP
                    end

                    return UDim2.new(1, -EDGE_MARGIN, 1, -(EDGE_MARGIN + offset))
                end
                local function restack()
                    for index, toast in toasts do
                        if not toast.leaving then
                            Animate.tween(toast.frame, {
                                Position = slotPosition(index),
                            }, Animate.Glide)
                        end
                    end
                end
                local function dismiss(toast: Toast__DARKLUA_TYPE_I)
                    if toast.leaving then
                        return
                    end

                    toast.leaving = true

                    local index = table.find(toasts, toast)

                    if index then
                        table.remove(toasts, index)
                    end

                    toast.shadow:Render(false)
                    Fade.hide(toast.frame)
                    Animate.tween(toast.frame, {
                        Position = toast.frame.Position + UDim2.fromOffset(ENTER_OFFSET, 0),
                    }, Animate.Smooth)
                    task.delay(0.3, function()
                        toast.frame:Destroy()
                    end)
                    restack()
                end
                local function notify(
                    title: string,
                    body: string?,
                    duration: number?
                )
                    local height = if body then 56 else 42
                    local frame = UI.new('Frame', {
                        Name = 'Toast',
                        AnchorPoint = Vector2.new(1, 1),
                        BackgroundTransparency = 0.04,
                        BorderSizePixel = 0,
                        Size = UDim2.new(0, WIDTH, 0, height),
                        ZIndex = 100,
                        Parent = rootGui,
                    })::Frame

                    Theme.bind(frame, 'BackgroundColor3', 'Popup')
                    UI.corner(frame, 10)
                    UI.stroke(frame, Color3.fromRGB(46, 46, 56), 0.5)

                    local shadow = Shadow.create(frame)

                    shadow:Render(true)

                    local accent = UI.new('Frame', {
                        Name = 'Accent',
                        BorderSizePixel = 0,
                        Position = UDim2.new(0, 10, 0, 10),
                        Size = UDim2.new(0, 3, 0, height - 20),
                        ZIndex = 101,
                        Parent = frame,
                    })::Frame

                    Theme.bind(accent, 'BackgroundColor3', 'Accent')
                    UI.corner(accent, 2)
                    UI.new('TextLabel', {
                        Name = 'Title',
                        BackgroundTransparency = 1,
                        Position = UDim2.new(0, 24, 0, if body then 9 else 0),
                        Size = UDim2.new(0, WIDTH - 36, 0, if body then 16 else height),
                        ZIndex = 101,
                        FontFace = Theme.Font.SemiBold,
                        Text = title,
                        TextColor3 = Theme.Color.Text,
                        TextSize = 12,
                        TextXAlignment = Enum.TextXAlignment.Left,
                        TextTruncate = Enum.TextTruncate.AtEnd,
                        Parent = frame,
                    })

                    if body then
                        UI.new('TextLabel', {
                            Name = 'Body',
                            BackgroundTransparency = 1,
                            Position = UDim2.new(0, 24, 0, 28),
                            Size = UDim2.new(0, WIDTH - 36, 0, 14),
                            ZIndex = 101,
                            FontFace = Theme.Font.Medium,
                            Text = body,
                            TextColor3 = Theme.Color.Muted,
                            TextSize = 11,
                            TextXAlignment = Enum.TextXAlignment.Left,
                            TextTruncate = Enum.TextTruncate.AtEnd,
                            Parent = frame,
                        })
                    end

                    local toast: Toast__DARKLUA_TYPE_I = {
                        frame = frame,
                        shadow = shadow,
                        height = height,
                        leaving = false,
                    }

                    table.insert(toasts, toast)

                    if #toasts > MAX_STACK then
                        dismiss(toasts[1])
                    end

                    local target = slotPosition(#toasts)

                    frame.Position = target + UDim2.fromOffset(ENTER_OFFSET, 0)

                    Fade.show(frame)
                    Animate.tween(frame, {Position = target}, Animate.Glide)
                    restack()
                    task.delay(duration or 3.2, function()
                        dismiss(toast)
                    end)
                end

                return {notify = notify}
            end

            return Notifications
        end

        function __DARKLUA_BUNDLE_MODULES.A(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.A

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.A = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local Theme = __DARKLUA_BUNDLE_MODULES.d()
            local UI = __DARKLUA_BUNDLE_MODULES.e()
            local searchRows: {SearchEntry__DARKLUA_TYPE_J} = {}
            local Row = {}

            function Row.base(
                panel: Instance,
                order: number,
                labelText: string,
                labelActive: boolean
            ): (Frame,TextLabel)
                local row = UI.new('Frame', {
                    Name = labelText:gsub('%s', '_'),
                    BackgroundTransparency = 1,
                    LayoutOrder = order,
                    Size = UDim2.new(1, 0, 0, 38),
                    Parent = panel,
                })::Frame
                local label = UI.new('TextLabel', {
                    Name = 'Label',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 14, 0, 0),
                    Size = UDim2.new(0, 200, 0, 38),
                    FontFace = labelActive and Theme.Font.SemiBold or Theme.Font.Medium,
                    Text = labelText,
                    TextColor3 = labelActive and Theme.Color.Text or Theme.Color.Muted,
                    TextSize = 12,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    Parent = row,
                })::TextLabel

                return row, label
            end
            function Row.divider(panel: Instance, order: number): Frame
                return UI.new('Frame', {
                    Name = 'Divider_' .. order,
                    BackgroundColor3 = Theme.Color.Divider,
                    BackgroundTransparency = 0.25,
                    BorderSizePixel = 0,
                    LayoutOrder = order,
                    Size = UDim2.new(1, 0, 0, 1),
                    Parent = panel,
                })::Frame
            end
            function Row.registerSearchable(
                row: Frame,
                divider: Frame?,
                text: string
            )
                table.insert(searchRows, {
                    row = row,
                    divider = divider,
                    text = text:lower(),
                })
            end
            function Row.filter(query: string)
                local q = query:lower()

                for _, entry in searchRows do
                    local show = q == '' or entry.text:find(q, 1, true) ~= nil

                    entry.row.Visible = show

                    if entry.divider then
                        entry.divider.Visible = show
                    end
                end
            end

            return Row
        end

        function __DARKLUA_BUNDLE_MODULES.B(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.B

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.B = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local Theme = __DARKLUA_BUNDLE_MODULES.d()
            local UI = __DARKLUA_BUNDLE_MODULES.e()
            local Animate = __DARKLUA_BUNDLE_MODULES.j()
            local Shadow = __DARKLUA_BUNDLE_MODULES.i()
            local State = __DARKLUA_BUNDLE_MODULES.c()
            local Row = __DARKLUA_BUNDLE_MODULES.B()
            local ToggleRow = {}

            function ToggleRow.create(
                panel: Instance,
                order: number,
                labelText: string,
                path: string,
                opts: ToggleOpts__DARKLUA_TYPE_K?,
                popups: PopupsApi__DARKLUA_TYPE_L
            ): Frame
                local options = opts or {}
                local state = State.get(path) and true or false
                local row, label = Row.base(panel, order, labelText, state)

                if options.dots then
                    UI.new('TextLabel', {
                        Name = 'Dots',
                        AnchorPoint = Vector2.new(1, 0.5),
                        BackgroundTransparency = 1,
                        Position = UDim2.new(1, -52, 0.460526, 0),
                        Size = UDim2.new(0, 20, 0, 33),
                        FontFace = Theme.Font.Bold,
                        Text = '\u{b7}\u{b7}\u{b7}',
                        TextColor3 = Color3.fromRGB(231, 231, 255),
                        TextSize = 14,
                        Parent = row,
                    })

                    local dotsBtn = UI.hitButton(row, UDim2.new(0, 28, 0, 38), UDim2.new(1, 
-78, 0, 0), 4)

                    dotsBtn.MouseButton1Click:Connect(function()
                        popups.KeybindPicker.open(row, path, options.bindName or labelText)
                    end)
                end
                if options.swatchPath then
                    local swatchPath = options.swatchPath
                    local swatch = UI.new('Frame', {
                        Name = 'ColorSwatch',
                        AnchorPoint = Vector2.new(1, 0.5),
                        BackgroundColor3 = State.get(swatchPath) or Color3.fromRGB(236, 236, 244),
                        BorderSizePixel = 0,
                        Position = UDim2.new(1, options.swatchOffset::number, 0.5, 0),
                        Size = UDim2.new(0, 16, 0, 16),
                        Parent = row,
                    })::Frame

                    UI.corner(swatch, 4)

                    local swatchBtn = UI.hitButton(row, UDim2.new(0, 24, 0, 24), UDim2.new(1, (options.swatchOffset::number) - 20, 0.5, 
-12), 4)

                    swatchBtn.MouseButton1Click:Connect(function()
                        popups.ColorPicker.open(swatch, swatchPath, labelText)
                    end)

                    State.Controls[swatchPath] = {
                        Set = function(value: any)
                            swatch.BackgroundColor3 = value

                            State.set(swatchPath, value)
                        end,
                        Get = function()
                            return State.get(swatchPath)
                        end,
                    }
                end

                local toggle = UI.new('Frame', {
                    Name = 'Toggle',
                    AnchorPoint = Vector2.new(1, 0.5),
                    BackgroundColor3 = Theme.Color.Toggle,
                    BorderSizePixel = 0,
                    Position = UDim2.new(1, -12, 0.5, 0),
                    Size = UDim2.new(0, 30, 0, 17),
                    Parent = row,
                })::Frame
                local toggleCorner = UI.corner(toggle)

                toggleCorner.CornerRadius = UDim.new(1, 0)

                local knob = UI.new('Frame', {
                    Name = 'Knob',
                    BackgroundColor3 = state and Theme.Color.Accent or Theme.Color.KnobOff,
                    BorderSizePixel = 0,
                    Position = state and UDim2.new(0, 15, 0, 2) or UDim2.new(0, 2, 0, 2),
                    Size = UDim2.new(0, 13, 0, 13),
                    Parent = toggle,
                })::Frame
                local knobCorner = UI.corner(knob)

                knobCorner.CornerRadius = UDim.new(1, 0)

                Shadow.create(knob):Render(true)

                local function render()
                    Animate.tween(knob, {
                        Position = state and UDim2.new(0, 15, 0, 2) or UDim2.new(0, 2, 0, 2),
                    }, Animate.Settle)
                    Animate.tween(knob, {
                        BackgroundColor3 = state and Theme.Color.Accent or Theme.Color.KnobOff,
                    }, Animate.Info)
                    Animate.tween(label, {
                        TextColor3 = state and Theme.Color.Text or Theme.Color.Muted,
                    }, Animate.Info)

                    label.FontFace = state and Theme.Font.SemiBold or Theme.Font.Medium
                end
                local function set(value: any, silent: boolean?)
                    local nextState = value and true or false

                    if nextState == state then
                        return
                    end

                    state = nextState

                    render()

                    if silent then
                        State.setSilent(path, state)
                    else
                        State.set(path, state)
                    end
                end

                row.BackgroundColor3 = Theme.Color.Divider

                local toggleHit = UI.hitButton(row, nil, nil, 2)

                toggleHit.MouseEnter:Connect(function()
                    Animate.tween(row, {BackgroundTransparency = 0.92}, Animate.Quick)
                end)
                toggleHit.MouseLeave:Connect(function()
                    Animate.tween(row, {BackgroundTransparency = 1}, Animate.Quick)
                end)
                toggleHit.MouseButton1Click:Connect(function()
                    set(not state)
                end)

                State.Controls[path] = {
                    Set = set,
                    Get = function()
                        return state
                    end,
                }

                return row
            end

            return ToggleRow
        end

        function __DARKLUA_BUNDLE_MODULES.C(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.C

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.C = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local Theme = __DARKLUA_BUNDLE_MODULES.d()
            local UI = __DARKLUA_BUNDLE_MODULES.e()
            local Drag = __DARKLUA_BUNDLE_MODULES.r()
            local Animate = __DARKLUA_BUNDLE_MODULES.j()
            local TableUtils = __DARKLUA_BUNDLE_MODULES.s()
            local State = __DARKLUA_BUNDLE_MODULES.c()
            local Row = __DARKLUA_BUNDLE_MODULES.B()
            local SliderRow = {}

            function SliderRow.create(
                panel: Instance,
                order: number,
                labelText: string,
                path: string,
                minValue: number,
                maxValue: number,
                width: number,
                format: string
            ): Frame
                local value = State.get(path) or minValue
                local row = Row.base(panel, order, labelText, false)
                local valueLabel = UI.new('TextLabel', {
                    Name = 'Value',
                    AnchorPoint = Vector2.new(1, 0.5),
                    BackgroundTransparency = 1,
                    Position = UDim2.new(1, -(12 + width + 8), 0.5, 0),
                    Size = UDim2.new(0, 49, 0, 38),
                    FontFace = Theme.Font.Medium,
                    Text = string.format(format, value),
                    TextColor3 = Theme.Color.Text,
                    TextSize = 12,
                    TextXAlignment = Enum.TextXAlignment.Right,
                    Parent = row,
                })::TextLabel
                local track = UI.new('Frame', {
                    Name = 'Slider',
                    AnchorPoint = Vector2.new(1, 0.5),
                    BackgroundColor3 = Theme.Color.Track,
                    BorderSizePixel = 0,
                    Position = UDim2.new(1, -12, 0.5, 0),
                    Size = UDim2.new(0, width, 0, 4),
                    Parent = row,
                })::Frame

                UI.corner(track, 2)

                local fill = UI.new('Frame', {
                    Name = 'Fill',
                    BackgroundColor3 = Theme.Color.Accent,
                    BorderSizePixel = 0,
                    Size = UDim2.new(0, 0, 0, 4),
                    Parent = track,
                })::Frame

                UI.corner(fill, 2)

                local knob = UI.new('Frame', {
                    Name = 'Knob',
                    AnchorPoint = Vector2.new(0.5, 0.5),
                    BackgroundColor3 = Color3.fromRGB(255, 255, 255),
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 0, 0.5, 0),
                    Size = UDim2.new(0, 10, 0, 10),
                    Parent = track,
                })::Frame
                local knobCorner = UI.corner(knob)

                knobCorner.CornerRadius = UDim.new(1, 0)

                UI.stroke(knob, Color3.fromRGB(30, 30, 38), 0, 2)

                local dragging = false

                local function render()
                    local alpha = (value - minValue) / (maxValue - minValue)
                    local px = TableUtils.round(alpha * width)
                    local info = if dragging then Animate.Fast else Animate.Smooth

                    Animate.tween(fill, {
                        Size = UDim2.new(0, px, 0, 4),
                    }, info)
                    Animate.tween(knob, {
                        Position = UDim2.new(0, px, 0.5, 0),
                    }, info)

                    valueLabel.Text = string.format(format, value)
                end
                local function set(nextValue: number, silent: boolean?)
                    nextValue = math.clamp(TableUtils.round(nextValue), minValue, maxValue)

                    if nextValue == value then
                        return
                    end

                    value = nextValue

                    render()

                    if silent then
                        State.setSilent(path, value)
                    else
                        State.set(path, value)
                    end
                end

                render()

                local hit = UI.hitButton(row, UDim2.new(0, width + 16, 0, 26), UDim2.new(1, 
-(12 + width + 8), 0.5, -13), 3)

                Drag.attachDrag(hit, function(position, began)
                    if began then
                        dragging = true

                        Animate.tween(knob, {
                            Size = UDim2.new(0, 14, 0, 14),
                        }, Animate.Quick)
                    end

                    local alpha = (position.X - track.AbsolutePosition.X) / math.max(1, track.AbsoluteSize.X)

                    set(minValue + math.clamp(alpha, 0, 1) * (maxValue - minValue))
                end, function()
                    dragging = false

                    Animate.tween(knob, {
                        Size = UDim2.new(0, 10, 0, 10),
                    }, Animate.Smooth)
                end)

                State.Controls[path] = {
                    Set = set,
                    Get = function()
                        return value
                    end,
                }

                return row
            end

            return SliderRow
        end

        function __DARKLUA_BUNDLE_MODULES.D(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.D

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.D = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local Theme = __DARKLUA_BUNDLE_MODULES.d()
            local UI = __DARKLUA_BUNDLE_MODULES.e()
            local Shadow = __DARKLUA_BUNDLE_MODULES.i()
            local Animate = __DARKLUA_BUNDLE_MODULES.j()
            local State = __DARKLUA_BUNDLE_MODULES.c()
            local Row = __DARKLUA_BUNDLE_MODULES.B()
            local DropdownRow = {}

            function DropdownRow.create(
                panel: Instance,
                order: number,
                labelText: string,
                path: string,
                options: {string},
                opts: DropdownOpts__DARKLUA_TYPE_M?,
                popups: PopupsApi__DARKLUA_TYPE_N
            ): Frame
                local o = opts or {}
                local current = State.get(path) or options[1]
                local row = Row.base(panel, order, labelText, false)

                if o.swatchPath then
                    local swatchPath = o.swatchPath
                    local swatch = UI.new('Frame', {
                        Name = 'ColorSwatch',
                        AnchorPoint = Vector2.new(1, 0.5),
                        BackgroundColor3 = State.get(swatchPath) or Color3.fromRGB(236, 236, 244),
                        BorderSizePixel = 0,
                        Position = UDim2.new(1, o.swatchOffset::number, 0.5, 0),
                        Size = UDim2.new(0, 16, 0, 16),
                        Parent = row,
                    })::Frame

                    UI.corner(swatch, 4)

                    local swatchBtn = UI.hitButton(row, UDim2.new(0, 24, 0, 24), UDim2.new(1, (o.swatchOffset::number) - 20, 0.5, 
-12), 4)

                    swatchBtn.MouseButton1Click:Connect(function()
                        popups.ColorPicker.open(swatch, swatchPath, labelText)
                    end)

                    State.Controls[swatchPath] = {
                        Set = function(value: any)
                            swatch.BackgroundColor3 = value

                            State.set(swatchPath, value)
                        end,
                        Get = function()
                            return State.get(swatchPath)
                        end,
                    }
                end

                local dropdown = UI.new('Frame', {
                    Name = 'Dropdown',
                    AnchorPoint = Vector2.new(1, 0.5),
                    BorderSizePixel = 0,
                    Position = UDim2.new(1, -12, 0.5, 0),
                    Size = UDim2.new(0, 58, 0, 22),
                    Parent = row,
                })::Frame

                Theme.bind(dropdown, 'BackgroundColor3', 'DropdownButton')
                UI.corner(dropdown, 6)
                Shadow.create(dropdown):Render(true)

                local valueLabel = UI.new('TextLabel', {
                    Name = 'Value',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 8, 0, 0),
                    Size = UDim2.new(0, 38, 0, 22),
                    FontFace = Theme.Font.Medium,
                    Text = current,
                    TextColor3 = Theme.Color.Text,
                    TextSize = 11,
                    TextXAlignment = Enum.TextXAlignment.Left,
                    TextTruncate = Enum.TextTruncate.AtEnd,
                    Parent = dropdown,
                })::TextLabel

                UI.new('ImageLabel', {
                    Name = 'IMG_Caret',
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 43, 0, 6),
                    Size = UDim2.new(0, 10, 0, 10),
                    Image = 'rbxassetid://10709790948',
                    ImageColor3 = Theme.Color.Muted,
                    ScaleType = Enum.ScaleType.Fit,
                    Parent = dropdown,
                })

                local function set(nextValue: string, silent: boolean?)
                    current = nextValue
                    valueLabel.Text = nextValue

                    if silent then
                        State.setSilent(path, nextValue)
                    else
                        State.set(path, nextValue)
                    end
                end

                local hoverStroke = UI.stroke(dropdown, Color3.fromRGB(70, 70, 86), 1, 1)
                local dropdownHit = UI.hitButton(dropdown, nil, nil, 3)

                dropdownHit.MouseEnter:Connect(function()
                    Animate.tween(hoverStroke, {Transparency = 0.35}, Animate.Quick)
                end)
                dropdownHit.MouseLeave:Connect(function()
                    Animate.tween(hoverStroke, {Transparency = 1}, Animate.Quick)
                end)
                dropdownHit.MouseButton1Click:Connect(function()
                    popups.Dropdown.open(dropdown, options, current, function(
                        picked
                    )
                        set(picked)
                    end)
                end)

                State.Controls[path] = {
                    Set = set,
                    Get = function()
                        return current
                    end,
                }

                return row
            end

            return DropdownRow
        end

        function __DARKLUA_BUNDLE_MODULES.E(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.E

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.E = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local Theme = __DARKLUA_BUNDLE_MODULES.d()
            local UI = __DARKLUA_BUNDLE_MODULES.e()
            local Drag = __DARKLUA_BUNDLE_MODULES.r()
            local Animate = __DARKLUA_BUNDLE_MODULES.j()
            local TableUtils = __DARKLUA_BUNDLE_MODULES.s()
            local State = __DARKLUA_BUNDLE_MODULES.c()
            local Row = __DARKLUA_BUNDLE_MODULES.B()
            local TRACK_WIDTH = 76
            local RangeRow = {}

            function RangeRow.create(
                panel: Instance,
                order: number,
                labelText: string,
                pathMin: string,
                pathMax: string,
                minValue: number,
                maxValue: number,
                format: string
            ): Frame
                local low = State.get(pathMin) or minValue
                local high = State.get(pathMax) or maxValue
                local row = Row.base(panel, order, labelText, false)
                local maxLabel = UI.new('TextLabel', {
                    Name = 'Max',
                    AnchorPoint = Vector2.new(1, 0.5),
                    BackgroundTransparency = 1,
                    Position = UDim2.new(1, -96, 0.5, 0),
                    Size = UDim2.new(0, 33, 0, 38),
                    FontFace = Theme.Font.Medium,
                    Text = string.format(format, high),
                    TextColor3 = Theme.Color.Text,
                    TextSize = 12,
                    TextXAlignment = Enum.TextXAlignment.Right,
                    Parent = row,
                })::TextLabel

                UI.new('TextLabel', {
                    Name = 'ArrowLR',
                    AnchorPoint = Vector2.new(1, 0.5),
                    BackgroundTransparency = 1,
                    Position = UDim2.new(1.05239, -131, 0.486842, 0),
                    Size = UDim2.new(0, 27, 0, 39),
                    FontFace = Theme.Font.Medium,
                    Text = '\u{2192}',
                    TextColor3 = Color3.fromRGB(105, 105, 116),
                    TextSize = 18,
                    Parent = row,
                })

                local minLabel = UI.new('TextLabel', {
                    Name = 'Min',
                    AnchorPoint = Vector2.new(1, 0.5),
                    BackgroundTransparency = 1,
                    Position = UDim2.new(1, -149, 0.5, 0),
                    Size = UDim2.new(0, 33, 0, 38),
                    FontFace = Theme.Font.Medium,
                    Text = string.format(format, low),
                    TextColor3 = Theme.Color.Text,
                    TextSize = 12,
                    TextXAlignment = Enum.TextXAlignment.Right,
                    Parent = row,
                })::TextLabel
                local track = UI.new('Frame', {
                    Name = 'RangeSlider',
                    AnchorPoint = Vector2.new(1, 0.5),
                    BackgroundColor3 = Theme.Color.Track,
                    BorderSizePixel = 0,
                    Position = UDim2.new(1, -12, 0.5, 0),
                    Size = UDim2.new(0, TRACK_WIDTH, 0, 4),
                    Parent = row,
                })::Frame

                UI.corner(track, 2)

                local fill = UI.new('Frame', {
                    Name = 'Fill',
                    BackgroundColor3 = Theme.Color.Accent,
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 11, 0, 0),
                    Size = UDim2.new(0, 38, 0, 4),
                    Parent = track,
                })::Frame

                UI.corner(fill, 2)

                local knobLo = UI.new('ImageLabel', {
                    Name = 'KnobLo',
                    AnchorPoint = Vector2.new(0.5, 0.5),
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 1, 0.5, 0),
                    Size = UDim2.new(0, 28, 0, 28),
                    Image = 'rbxassetid://122860942594944',
                    ScaleType = Enum.ScaleType.Fit,
                    Parent = track,
                })::ImageLabel
                local knobHi = UI.new('ImageLabel', {
                    Name = 'KnobHi',
                    AnchorPoint = Vector2.new(0.5, 0.5),
                    BackgroundTransparency = 1,
                    Position = UDim2.new(0, 41, 0.5, 0),
                    Rotation = 180,
                    Size = UDim2.new(0, 28, 0, 28),
                    Image = 'rbxassetid://122860942594944',
                    ScaleType = Enum.ScaleType.Fit,
                    Parent = track,
                })::ImageLabel
                local active: 'lo' | 'hi' | nil = nil

                local function render()
                    local alphaLo = (low - minValue) / (maxValue - minValue)
                    local alphaHi = (high - minValue) / (maxValue - minValue)
                    local loPx = TableUtils.round(alphaLo * TRACK_WIDTH)
                    local hiPx = TableUtils.round(alphaHi * TRACK_WIDTH)
                    local info = if active ~= nil then Animate.Fast else Animate.Smooth

                    Animate.tween(fill, {
                        Position = UDim2.new(0, loPx, 0, 0),
                        Size = UDim2.new(0, math.max(0, hiPx - loPx), 0, 4),
                    }, info)
                    Animate.tween(knobLo, {
                        Position = UDim2.new(0, loPx + 1, 0.5, 0),
                    }, info)
                    Animate.tween(knobHi, {
                        Position = UDim2.new(0, hiPx + 3, 0.5, 0),
                    }, info)

                    minLabel.Text = string.format(format, low)
                    maxLabel.Text = string.format(format, high)
                end

                render()

                local function setLow(nextValue: number, silent: boolean?)
                    nextValue = math.clamp(TableUtils.round(nextValue), minValue, high)

                    if nextValue == low then
                        return
                    end

                    low = nextValue

                    render()

                    if silent then
                        State.setSilent(pathMin, low)
                    else
                        State.set(pathMin, low)
                    end
                end
                local function setHigh(nextValue: number, silent: boolean?)
                    nextValue = math.clamp(TableUtils.round(nextValue), low, maxValue)

                    if nextValue == high then
                        return
                    end

                    high = nextValue

                    render()

                    if silent then
                        State.setSilent(pathMax, high)
                    else
                        State.set(pathMax, high)
                    end
                end

                local hit = UI.hitButton(row, UDim2.new(0, TRACK_WIDTH + 24, 0, 30), UDim2.new(1, 
-(12 + TRACK_WIDTH + 12), 0.5, -15), 3)

                Drag.attachDrag(hit, function(position, began)
                    local alpha = math.clamp((position.X - track.AbsolutePosition.X) / math.max(1, track.AbsoluteSize.X), 0, 1)
                    local value = minValue + alpha * (maxValue - minValue)

                    if began then
                        if value <= low then
                            active = 'lo'
                        elseif value >= high then
                            active = 'hi'
                        else
                            active = if(value - low) <= (high - value)then'lo'else'hi'
                        end

                        local grabbed = if active == 'lo'then knobLo else knobHi

                        Animate.tween(grabbed, {
                            Size = UDim2.new(0, 32, 0, 32),
                        }, Animate.Quick)
                    end
                    if active == 'lo' then
                        setLow(value)
                    else
                        setHigh(value)
                    end
                end, function()
                    active = nil

                    Animate.tween(knobLo, {
                        Size = UDim2.new(0, 28, 0, 28),
                    }, Animate.Smooth)
                    Animate.tween(knobHi, {
                        Size = UDim2.new(0, 28, 0, 28),
                    }, Animate.Smooth)
                end)

                State.Controls[pathMin] = {
                    Set = setLow,
                    Get = function()
                        return low
                    end,
                }
                State.Controls[pathMax] = {
                    Set = setHigh,
                    Get = function()
                        return high
                    end,
                }

                return row
            end

            return RangeRow
        end

        function __DARKLUA_BUNDLE_MODULES.F(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.F

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.F = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local Theme = __DARKLUA_BUNDLE_MODULES.d()
            local UI = __DARKLUA_BUNDLE_MODULES.e()
            local Shadow = __DARKLUA_BUNDLE_MODULES.i()
            local Row = __DARKLUA_BUNDLE_MODULES.B()
            local ToggleRow = __DARKLUA_BUNDLE_MODULES.C()
            local SliderRow = __DARKLUA_BUNDLE_MODULES.D()
            local DropdownRow = __DARKLUA_BUNDLE_MODULES.E()
            local RangeRow = __DARKLUA_BUNDLE_MODULES.F()
            local AutoTotemPanel = {}

            function AutoTotemPanel.create(parent: Instance, popups: any): AutoTotemPanelRefs__DARKLUA_TYPE_O
                local main = UI.new('Frame', {
                    Name = 'AutoTotem_main',
                    AutomaticSize = Enum.AutomaticSize.Y,
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 166, 0, 118),
                    Size = UDim2.new(0, 264, 0, 0),
                    Parent = parent,
                })::Frame

                Theme.bind(main, 'BackgroundColor3', 'Panel')
                UI.corner(main, 10)
                Shadow.create(main):Render(true)
                UI.new('UIListLayout', {
                    SortOrder = Enum.SortOrder.LayoutOrder,
                    Parent = main,
                })

                local extra = UI.new('Frame', {
                    Name = 'AutoTotem_extra',
                    AutomaticSize = Enum.AutomaticSize.Y,
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 444, 0, 118),
                    Size = UDim2.new(0, 262, 0, 0),
                    Parent = parent,
                })::Frame

                Theme.bind(extra, 'BackgroundColor3', 'Panel')
                UI.corner(extra, 10)
                Shadow.create(extra):Render(true)
                UI.new('UIListLayout', {
                    SortOrder = Enum.SortOrder.LayoutOrder,
                    Parent = extra,
                })

                local enableRow = ToggleRow.create(main, 2, 'AutoTotem enable', 'AutoTotem.Enabled', {
                    dots = true,
                    bindName = 'Enable AutoTotem',
                }, popups)

                Row.registerSearchable(enableRow, Row.divider(main, 3), 'AutoTotem enable')

                local healthRow = SliderRow.create(main, 4, 'AutoTotem health', 'AutoTotem.Health', 0, 100, 75, '%d%%')

                Row.registerSearchable(healthRow, Row.divider(main, 5), 'AutoTotem health')

                local modeRow = DropdownRow.create(main, 6, 'AutoTotem mode', 'AutoTotem.Mode', {
                    'Smart',
                    'Always',
                    'Legit',
                }, {
                    swatchOffset = -79,
                    swatchPath = 'AutoTotem.ModeColor',
                }, popups)

                Row.registerSearchable(modeRow, Row.divider(main, 7), 'AutoTotem mode')

                local combatRow = ToggleRow.create(main, 8, 'AutoTotem only combat', 'AutoTotem.OnlyCombat', {
                    swatchOffset = -51,
                    swatchPath = 'AutoTotem.CombatColor',
                }, popups)

                Row.registerSearchable(combatRow, Row.divider(main, 9), 'AutoTotem only combat')

                local packetRow = ToggleRow.create(main, 10, 'AutoTotem packet', 'AutoTotem.Packet', nil, popups)

                Row.registerSearchable(packetRow, Row.divider(main, 11), 'AutoTotem packet')

                local distanceRow = RangeRow.create(main, 12, 'Distance', 'AutoTotem.DistanceMin', 'AutoTotem.DistanceMax', 0, 90, '%d\u{b0}')

                Row.registerSearchable(distanceRow, nil, 'Distance')

                local delayRow = SliderRow.create(extra, 2, 'AutoTotem delay', 'AutoTotem.Delay', 0, 500, 57, '%d ms')

                Row.registerSearchable(delayRow, Row.divider(extra, 3), 'AutoTotem delay')

                local priorityRow = DropdownRow.create(extra, 4, 'AutoTotem priority', 'AutoTotem.Priority', {
                    'Health',
                    'Armor',
                    'Distance',
                }, nil, popups)

                Row.registerSearchable(priorityRow, Row.divider(extra, 5), 'AutoTotem priority')

                local hotbarRow = ToggleRow.create(extra, 6, 'AutoTotem hotbar only', 'AutoTotem.HotbarOnly', nil, popups)

                Row.registerSearchable(hotbarRow, Row.divider(extra, 7), 'AutoTotem hotbar only')

                local smartSwitchRow = ToggleRow.create(extra, 8, 'AutoTotem smart switch', 'AutoTotem.SmartSwitch', nil, popups)

                Row.registerSearchable(smartSwitchRow, Row.divider(extra, 9), 'AutoTotem smart switch')

                local switchDelayRow = SliderRow.create(extra, 10, 'AutoTotem switch delay', 'AutoTotem.SwitchDelay', 0, 1000, 40, '%d ms')

                Row.registerSearchable(switchDelayRow, nil, 'AutoTotem switch delay')

                return {
                    main = main,
                    extra = extra,
                }
            end

            return AutoTotemPanel
        end

        function __DARKLUA_BUNDLE_MODULES.G(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.G

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.G = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local Theme = __DARKLUA_BUNDLE_MODULES.d()
            local UI = __DARKLUA_BUNDLE_MODULES.e()
            local Animate = __DARKLUA_BUNDLE_MODULES.j()
            local State = __DARKLUA_BUNDLE_MODULES.c()
            local CARD_WIDTH = 207
            local CARD_HEIGHT = 131
            local CARD_GAP = 16
            local STYLE_PATH = 'Theme.Style'
            local THEME_CARDS = {
                {
                    style = 'Gray',
                    image = 'rbxassetid://115004254108545',
                },
                {
                    style = 'Black',
                    image = 'rbxassetid://100789703266413',
                },
            }
            local ThemePanel = {}

            function ThemePanel.create(parent: Instance): ThemePanelRefs__DARKLUA_TYPE_P
                local main = UI.new('Frame', {
                    Name = 'Theme_main',
                    BackgroundTransparency = 1,
                    BorderSizePixel = 0,
                    Position = UDim2.new(0, 166, 0, 118),
                    Size = UDim2.new(0, CARD_WIDTH * 2 + CARD_GAP, 0, CARD_HEIGHT),
                    Parent = parent,
                })::Frame
                local extra = UI.new('Frame', {
                    Name = 'Theme_extra',
                    BackgroundTransparency = 1,
                    BorderSizePixel = 0,
                    Size = UDim2.new(),
                    Parent = parent,
                })::Frame
                local strokes: {[string]: UIStroke} = {}

                local function renderSelection()
                    local current = State.get(STYLE_PATH)

                    for style, stroke in strokes do
                        Animate.tween(stroke, {
                            Transparency = if style == current then 0 else 1,
                        }, Animate.Quick)
                    end
                end

                for index, card in THEME_CARDS do
                    local frame = UI.new('Frame', {
                        Name = 'ThemeCard_' .. card.style,
                        BackgroundColor3 = Theme.Color.Panel,
                        BorderSizePixel = 0,
                        Position = UDim2.new(0, (index - 1) * (CARD_WIDTH + CARD_GAP), 0, 0),
                        Size = UDim2.new(0, CARD_WIDTH, 0, CARD_HEIGHT),
                        Parent = main,
                    })::Frame

                    UI.corner(frame, 10)

                    local stroke = UI.stroke(frame, Theme.Color.Accent, 1, 2)

                    strokes[card.style] = stroke

                    local image = UI.new('ImageLabel', {
                        Name = 'IMG_Preview',
                        BackgroundTransparency = 1,
                        BorderSizePixel = 0,
                        Size = UDim2.new(1, 0, 1, 0),
                        Image = card.image,
                        Parent = frame,
                    })::ImageLabel

                    UI.corner(image, 10)

                    local hit = UI.hitButton(frame, nil, nil, 3)

                    hit.MouseEnter:Connect(function()
                        if State.get(STYLE_PATH) ~= card.style then
                            Animate.tween(stroke, {Transparency = 0.7}, Animate.Quick)
                        end
                    end)
                    hit.MouseLeave:Connect(function()
                        if State.get(STYLE_PATH) ~= card.style then
                            Animate.tween(stroke, {Transparency = 1}, Animate.Quick)
                        end
                    end)
                    hit.MouseButton1Click:Connect(function()
                        State.set(STYLE_PATH, card.style)
                        Theme.setStyle(card.style)
                        renderSelection()
                    end)
                end

                State.Controls[STYLE_PATH] = {
                    Set = function(value: any)
                        State.set(STYLE_PATH, value)
                        Theme.setStyle(value)
                        renderSelection()
                    end,
                    Get = function()
                        return State.get(STYLE_PATH)
                    end,
                }

                renderSelection()

                return {
                    main = main,
                    extra = extra,
                }
            end

            return ThemePanel
        end

        function __DARKLUA_BUNDLE_MODULES.H(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.H

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.H = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local Animate = __DARKLUA_BUNDLE_MODULES.j()
            local Fade = __DARKLUA_BUNDLE_MODULES.n()
            local Shadow = __DARKLUA_BUNDLE_MODULES.i()
            local InputManager = __DARKLUA_BUNDLE_MODULES.u()
            local WindowManager = {}

            function WindowManager.create(
                window: Frame,
                scale: UIScale,
                onToggle: ((open:boolean) -> ())?
            ): WindowManagerApi__DARKLUA_TYPE_Q
                local open = true
                local shadow = Shadow.create(window)
                local toggleToken = 0

                local function setOpen(nextOpen: boolean)
                    open = nextOpen

                    toggleToken += 1

                    local token = toggleToken

                    if onToggle then
                        onToggle(open)
                    end
                    if open then
                        window.Visible = true
                        scale.Scale = 0.95

                        Animate.tween(scale, {Scale = 1}, Animate.Glide)
                        Fade.show(window)
                        shadow:Render(true, 0.42)
                        task.delay(0.45, function()
                            if open and toggleToken == token then
                                shadow:Render(true, 0.62)
                            end
                        end)
                    else
                        Animate.tween(scale, {Scale = 0.97}, Animate.Smooth)
                        Fade.hide(window)
                        shadow:Render(false)
                        task.delay(0.26, function()
                            if not open then
                                window.Visible = false
                            end
                        end)
                    end
                end

                InputManager.onMenuToggle(function()
                    setOpen(not open)
                end)
                setOpen(true)

                return {
                    isOpen = function()
                        return open
                    end,
                    setOpen = setOpen,
                }
            end

            return WindowManager
        end

        function __DARKLUA_BUNDLE_MODULES.I(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.I

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.I = v
            end

            return v.c
        end
    end
    do
        local function __modImpl()
            local RunService = game:GetService('RunService')
            local StatsService = game:GetService('Stats')
            local Connections = __DARKLUA_BUNDLE_MODULES.a()
            local TableUtils = __DARKLUA_BUNDLE_MODULES.s()
            local UPDATE_INTERVAL = 0.5
            local StatsManager = {}

            function StatsManager.start(
                fpsLabel: TextLabel,
                pingLabel: TextLabel
            )
                local frames = 0
                local accumulated = 0

                Connections.bind(RunService.RenderStepped, function(
                    dt: number
                )
                    frames += 1
                    accumulated += dt

                    if accumulated < UPDATE_INTERVAL then
                        return
                    end

                    fpsLabel.Text = string.format('%d FPS', TableUtils.round(frames / accumulated))
                    frames, accumulated = 0, 0

                    local ok, ping = pcall(function()
                        return StatsService.Network.ServerStatsItem['Data Ping']:GetValue()
                    end)

                    pingLabel.Text = if ok then string.format('%d PING', TableUtils.round(ping))else'-- PING'
                end)
            end

            return StatsManager
        end

        function __DARKLUA_BUNDLE_MODULES.J(): typeof(__modImpl())
            local v = __DARKLUA_BUNDLE_MODULES.cache.J

            if not v then
                v = {
                    c = __modImpl(),
                }
                __DARKLUA_BUNDLE_MODULES.cache.J = v
            end

            return v.c
        end
    end
end

local Players = game:GetService('Players')
local CoreGui = game:GetService('CoreGui')
local Connections = __DARKLUA_BUNDLE_MODULES.a()
local State = __DARKLUA_BUNDLE_MODULES.c()
local Types = __DARKLUA_BUNDLE_MODULES.b()
local Watermark = __DARKLUA_BUNDLE_MODULES.f()
local ItemGrid = __DARKLUA_BUNDLE_MODULES.g()
local ArmorHotbar = __DARKLUA_BUNDLE_MODULES.h()
local MainWindow = __DARKLUA_BUNDLE_MODULES.k()
local Nav = __DARKLUA_BUNDLE_MODULES.m()
local Tabs = __DARKLUA_BUNDLE_MODULES.o()
local Popups = __DARKLUA_BUNDLE_MODULES.x()
local Presets = __DARKLUA_BUNDLE_MODULES.y()
local KeybindList = __DARKLUA_BUNDLE_MODULES.z()
local Notifications = __DARKLUA_BUNDLE_MODULES.A()
local AutoTotemPanel = __DARKLUA_BUNDLE_MODULES.G()
local ThemePanel = __DARKLUA_BUNDLE_MODULES.H()
local Row = __DARKLUA_BUNDLE_MODULES.B()
local Drag = __DARKLUA_BUNDLE_MODULES.r()
local InputManager = __DARKLUA_BUNDLE_MODULES.u()
local WindowManager = __DARKLUA_BUNDLE_MODULES.I()
local StatsManager = __DARKLUA_BUNDLE_MODULES.J()
local LocalPlayer = Players.LocalPlayer
local guiParent: Instance

do
    local ok, hui = pcall(function()
        return (gethui::any) and (gethui::any)()
    end)

    if ok and hui then
        guiParent = hui
    else
        local ok2, coreGui = pcall(function()
            local probe = Instance.new('Folder')

            probe.Parent = CoreGui

            probe:Destroy()

            return CoreGui
        end)

        guiParent = (ok2 and coreGui) or LocalPlayer:WaitForChild('PlayerGui')
    end
end
do
    local existing = guiParent:FindFirstChild('WindsUI')

    if existing then
        existing:Destroy()
    end
end

local rootGui = Instance.new('ScreenGui')

rootGui.Name = 'WindsUI'
rootGui.ResetOnSpawn = false
rootGui.ScreenInsets = Enum.ScreenInsets.DeviceSafeInsets
rootGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local TAB_MODULES: {[string]: {ModuleDefinition__DARKLUA_TYPE_k}} = {
    Combat = {
        {
            name = 'AutoTotem',
            caret = true,
        },
        {
            name = 'Killaura',
            caret = true,
        },
        {
            name = 'Velocity',
        },
        {
            name = 'Criticals',
        },
        {
            name = 'AntiKnockback',
        },
    },
    Movement = {
        {
            name = 'Bhop',
        },
        {
            name = 'Fly',
            caret = true,
        },
        {
            name = 'Speed',
        },
        {
            name = 'NoFall',
        },
    },
    Visuals = {
        {
            name = 'ESP',
            caret = true,
        },
        {
            name = 'Tracers',
        },
        {
            name = 'NameTags',
        },
        {
            name = 'ChestESP',
            caret = true,
        },
        {
            name = 'ItemESP',
        },
        {
            name = 'XRay',
            caret = true,
        },
        {
            name = 'Fullbright',
            caret = true,
        },
        {
            name = 'TimeChanger',
        },
        {
            name = 'Weather',
            caret = true,
        },
    },
    Render = {
        {
            name = 'Ambience',
            caret = true,
        },
        {
            name = 'FOV',
        },
        {
            name = 'CameraFX',
            caret = true,
        },
    },
    Misc = {
        {
            name = 'AntiAFK',
        },
        {
            name = 'AutoChat',
        },
        {
            name = 'Spinbot',
        },
    },
    Theme = {
        {
            name = 'Accent',
            caret = true,
        },
        {
            name = 'Background',
        },
    },
    ['Auto-Buy'] = {
        {
            name = 'Kits',
            caret = true,
        },
        {
            name = 'Potions',
        },
    },
}

InputManager.start()

local watermarkRefs = Watermark.create(rootGui)

StatsManager.start(watermarkRefs.FPSLabel, watermarkRefs.PingLabel)

local itemGrid = ItemGrid.create(rootGui)
local armorHotbar = ArmorHotbar.create(rootGui)
local mainRefs = MainWindow.create(rootGui)
local tabs: TabsController__DARKLUA_TYPE_w
local keybindList: KeybindListApi__DARKLUA_TYPE_G

local function getSelectedModule(tab: string): string?
    return if tabs then tabs.currentModule(tab)else nil
end
local function onModuleSelected(tab: string, moduleName: string)
    if tabs then
        tabs:selectModule(tab, moduleName)
    end
end
local function onNewBind(
    path: string,
    name: string,
    key: Enum.KeyCode,
    mode: string
)
    if keybindList then
        keybindList.addBind(path, name, key, mode::BindMode__DARKLUA_TYPE_h)
    end
end

local popups = Popups.create(mainRefs.Window, TAB_MODULES, getSelectedModule, onModuleSelected, onNewBind)
local autoTotemPanel = AutoTotemPanel.create(mainRefs.Window, popups)
local themePanel = ThemePanel.create(mainRefs.Window)
local navRefs = Nav.create(mainRefs.LeftPanel, function(tab)
    if tabs then
        tabs:selectTab(tab)
    end
end, function(tab, navFrame)
    if tabs then
        tabs:selectTab(tab)
    end

    popups.ModuleSelector.open(tab, navFrame)
end)

tabs = Tabs.create({
    navRefs = navRefs,
    crumbSection = mainRefs.CrumbSection,
    crumbElement = mainRefs.CrumbElement,
    mainTitle = mainRefs.MainTitle,
    extraTitle = mainRefs.ExtraTitle,
    tabModules = TAB_MODULES,
    panels = {
        ['Combat.AutoTotem'] = autoTotemPanel,
        ['Theme.Accent'] = themePanel,
    },
})
keybindList = KeybindList.create(rootGui)

Presets.create(mainRefs.Window, mainRefs.PresetButton, mainRefs.PresetButtonLabel)

local notifications = Notifications.create(rootGui)

mainRefs.SearchBox:GetPropertyChangedSignal('Text'):Connect(function()
    Row.filter(mainRefs.SearchBox.Text)
end)

local windowManager = WindowManager.create(mainRefs.Window, mainRefs.Scale, function(
    _open: boolean
)
    popups.closeAll()
end)

Drag.makeDraggable(watermarkRefs.Frame)
Drag.makeDraggable(itemGrid)
Drag.makeDraggable(armorHotbar)
Drag.makeDraggable(mainRefs.Window, mainRefs.TopBar)
Drag.makeDraggable(mainRefs.Window, mainRefs.Logo)

rootGui.Parent = guiParent

notifications.notify('WindsUI', 'Loaded successfully')

local WindsUI = {}

WindsUI.Gui = rootGui
WindsUI.Config = State.Config
WindsUI.Controls = State.Controls

function WindsUI.Notify(title: string, body: string?, duration: number?)
    notifications.notify(title, body, duration)
end
function WindsUI.OnChanged(callback: ChangedListener__DARKLUA_TYPE_c)
    State.onChanged(callback)
end
function WindsUI.SetOpen(open: boolean)
    windowManager.setOpen(open)
end
function WindsUI.Destroy()
    Connections.disconnectAll()
    rootGui:Destroy()
end

local env = (typeof(getgenv) == 'function' and (getgenv::any)()) or _G
local target: any = env

target.WindsUI = WindsUI

return WindsUI
